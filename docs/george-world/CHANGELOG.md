# GEORGE WORLD — CHANGELOG

Rewritten as CHANGELOG.md 2026-09-01 17:40 from GEORGE_WORLD_BUGS.md (decision 3).

**Rules:** append-only · one entry per version · newest at top. Every entry carries the commit hash and the VERIFICATION notes (what was checked, the numbers, the root causes) — never a restated commit subject. The version + game commit hash also goes into docs/george-world/STATUS.md. Code-side edits only.

## v0.80 — HvB title splash on first entry (built 2026-09-03; committed 2026-09-04 by the hygiene sweep, commit 0b9c9c1)
**Unverified backfill by DISPATCH.** The v0.80 title/badge bump and a full-screen HvB title splash on first entry (`assets/hvb_title.png`) were built by the stage session on 2026-09-03 and served publicly for a day uncommitted; committed as found on 2026-09-04 after a `node --check` of the module. No verification notes exist for the splash itself. Same sweep committed HvB Asset Stage v8.40 (c593b7f: Meshy-first catalog, creature + scenes studies, pre-Meshy skins archived, low-poly dragon) and the fire-FX study, asset audit page and Doug's stage notes (9cb2bec).

## v0.79 — + THE STAGE: weather testing on the real island (2026-08-31 17:16, commit 399de7f)
**Unverified backfill from the commit message.**
- v0.79: a WX TEST menu beside NEW GAME forces the island's actual sky — fair / clear / overcast / STORM, the same states the daily roll uses, so storm work-dampening and palm-lean come with it; AUTO hands the sky back to the dice.
- stage.html: ONE screen for every object and animation — a grouped pulldown loads any of the 27 studies (or the live game itself) into the stage; each study's own animation controls appear inside its panel so nothing clutters. Served at :8201/stage.html.
- Commit note: the algorithmic wave sim is still study-only (waves_study) — integrating it onto the island's real coastline is the next big build; the WX TEST menu covers the weather half on the real island today.
- Files: georgeworld.html (+22/−2), stage.html (+86, new).

## v0.78 — more berries: 8 village-facing, 20 over the ridge in clumps (2026-08-31 16:32, commit dacc2a5)
**Unverified backfill from the commit message.**
- Doug's distribution: EIGHT bushes on the hillside facing the first village (5 at the twin hills + 3 in the king's-hill/volcano gap) and TWENTY beyond the ridge in five little clumps of four (rich, far).
- Still hillside-only, never on the beach; the far side is still the prize worth the walk. (Supersedes the v0.61/v0.66 4+3+16 layout.)
- Files: georgeworld.html (+24/−20).

## v0.77 — steady palm shadows (the tremble, fixed at the light) (2026-08-31 14:25, commit c73a8c0)
**Unverified backfill from the commit message.**
- Root cause of the constant shadow flicker: the sun light GLIDES with the day cycle (~1.6°/s at 1×), so the shadow map re-rasterizes every thin frond into different texels every single frame — the whole floor shimmers.
- Fix: the light's angle now advances in ~1.7° quantized steps (shadows hold perfectly still between hops) while the visible sun and moon discs keep gliding smoothly.
- Shadow map doubled to 2048×2048 for less texel crawl on fronds; night moonlight stepped the same way.
- Files: georgeworld.html (+20/−7).

## v0.76 — real sleep: down on the ground, on a side or back (2026-08-31 14:23, commit 2562640)
**Unverified backfill from the commit message.**
- Sleepers no longer tilt-and-hover (Doug's report): the body goes all the way DOWN — rotation to a full 1.52 side lie, or flat on the BACK for ~30% — resting against the ground; the old +0.4 hover cut to the body's half-depth.
- Limbs posed like a real sleeper: top knee drawn up and bottom leg long (never parallel-stacked), both arms out in FRONT of the body (nobody sleeps on an arm), back-sleepers sprawled open with knees flopped apart, and a slow breath in the chest.
- Pose assigned on falling asleep, tidied on rising; the bow still owns rotation.x while live.
- Files: georgeworld.html (+48/−6).

## v0.75 — the LANDING BEWILDERMENT + everybody has a history (2026-08-31 14:22, commit f2b0283)
**Unverified backfill from the commit message.**
- The first ~3 sim-minutes of a world belong to the confusion (Doug): strangers on unknown sand remark how unexpected, weird and scary it is, and speculate in pairs — heaven? taken by a saucer? is home a false memory?
- INTRODUCTIONS: each Vita now carries a little history tidbit (kept bees, mended nets, sang at weddings, swept a lighthouse…) surfaced once through the new `landing_intro` lines.
- Two new pool categories (`landing` ×10, `landing_intro` ×4) in the repo dialogue file AND the Dropbox master.
- The window is pure world-age (`histClock < 170`), so a rewind to the start replays the wonder; it owns the airwaves like night does.
- Files: george_world_dialogue.js (+23), georgeworld.html (+57/−18).

---
*Everything below is the original GEORGE_WORLD_BUGS.md, verbatim and in its original order (newest at top), carried over 2026-09-01 17:40. Only the v0.18-era "To verify" tail was removed.*

# George World — bug & polish punch-list

## Closed in v0.66–v0.74 (the go-for-all run: open list finished + Doug's live batch, 2026-08-31 ~14:10, HEAD 8fb1af2)
- [x] **v0.66 `d0e961c` — item 5 VERIFIED:** placement spec conforms in full (palms beach/just-inland; bushes hillside-only 4+3+16; 0.7× scale; Joshua-tree + carambola; opening tasting targets the near hillside bushes). Fixed two leftover "shore bushes / shore pickings" chronicle wordings.
- [x] **v0.67 `5e9f7a4` — item 7:** discovery order FLIPPED — the zoom-in watch-it-being-MADE story plays FIRST (camera on the maker, world at 1×) and the DISCOVERY card caps it when the story ends. A killed story (rewind) takes its waiting card with it; a tech with no eligible star shows the card directly. (Sleep-guard half was already in, v0.61.)
- [x] **v0.68 `0380c70` — item 8:** the scene bar became the substantial STATUS PANEL — yellow headline / white details, bigger, slides up on every change, idle lines stay small and calm. **GEORGEOTRON rename swept** (G button, help, panel header, hints); *decision for Doug to veto: the page/view = GEORGEOTRON, the machine itself keeps the name Georgeomat* (DESIGN's open reconcile).
- [x] **v0.69 `ebe6341` — item 9:** Poppy's PLAY URGE — every few sim-minutes she runs off and pesters a random non-favorite Vita to play (orbiting waypoints, constant play-hops, full-speed tail, the mark exults, chronicled). The favorite stays the bond.
- [x] **v0.70 `4a8d62a` — Doug live:** nine DISTINCT hair colors (black→silver) and personal SHIRT colors for the unclaimed — a claim/promotion still dyes to house color/purple, so the domination spread reads louder.
- [x] **v0.71 `0ca621a` — figs→berries dialogue sweep:** tender/trade/smalltalk/gossip/discovery_benefit/meeting lines all speak berries now (trade barters green-for-ripe); build_labor dropped its palm-timber wording (firs since v0.63). **Dropbox master copy swept identically — both copies in sync.**
- [x] **v0.72 `df3dc35` — Doug live, the WALK batch:** (1) floating shoes — body presses 0.09 into the analytic height so soles meet the rendered ground on slopes; (2) skating feet — gait phase = accumulated DISTANCE, strides match ground covered; (3) marching in place — swing amplitude scales with real speed, a blocked walker stands; (4) POPPY PITCHES TO THE CONTOUR — nose up climbing, nose down descending, eased, zeroed when curled up.
- [x] **v0.73 `4105372` — Doug live: ONE bottom panel.** The status line is the TITLE of what's going on; the report (hints/warnings/story beats) fills in under it inside the same box. No more overlapping boxes.
- [x] **v0.74 `8fb1af2` — Doug live: a death is a BIG TO-DO.** Somber twin of the discovery card (dark slate, candles, no confetti) with who they were: rank, day, berries gathered, who they served, a temperament line. **THE GRAVEYARD:** graves lie in neat rows (4/row) on one quiet slope instead of the dying-spot — funerals convene there — and graves are SOLID (obstacle per grave; walkers steer around, Poppy vaults; no more clipping through headstones).

## Closed in v0.65 (BUILDING IS A BIG DEAL — the communal RAISING, 2026-08-31 13:51, commit b053e6f)
- [x] **WHO BUILDS WHAT (DESIGN 08-31, open-list item 4):** a duke's house (and his lean-to) is raised by HIS OWN serfs — his retinue and nobody else's; the **KING'S HALL is raised by ALL serfs** with NO crew cap (every willing hand on the island); the communal lean-to by the unclaimed serfs it shelters. `mayBuild(v, project)` gates both the felling and the raising. `activeProject` now skips a project nobody may build, so a duke whose retinue emptied can't jam the whole construction queue.
- [x] **THE RAISING — the signature communal spectacle, camera on it:** the moment serf hands land on the hall, the announcements box calls "🏛 THE RAISING — every serf to the king's hill!", the chronicle logs it, the SCENE BAR headline flips to "THE RAISING — the whole island lifts the KING'S HALL" (which cuts the idle director to it, per the v0.48 rule), and the director's POI ranking holds the shot on the hill just under court/eruption/meetings while hands stay on the frame. With every serf lifting, the hall visibly RISES FAST — many hands, one roof.
- [x] **CROWD, NOT DOGPILE:** every builder takes a slot on a ring around the site (radius ~3.8, own angle) instead of the old single shared spot — a full-cast hall crew reads as a crowd raising one roof. Finish beat: a big burst over the hall and every serf within 28 units throws their arms up (exult) — the one moment the king's hall belongs to everybody who lifted it. (v0.64's building_scene chatter runs through the whole raising.)

## Closed in v0.64 (the DIALOGUE WIRING batch — six silent categories come alive, 2026-08-31 13:46, commit 8ba6121)
- [x] **FUNERAL (serfs-only, DESIGN 08-31):** a dead serf queues a rite; come daylight up to 4 of the nearest free serfs walk to the grave, ring it, BOW (the ceremony bow + wail face), and one speaks a line from the `funeral` pool ("We bury our own. It's always been us."). The Dukes and the King NEVER attend — and the chronicle says so: "🕊 the serfs gather at NAME's grave — the Dukes do not come. They never do." Night deaths wait for dawn; one rite at a time; transient under rewind (a scrubbed death dissolves the rite). Poppy's graveside grief (v0.36) now has company.
- [x] **MORNING (DESIGN 08-31):** dawn opens a ~55 sim-s window where waking talk comes BEFORE regular chatter (the window owns the airwaves, like night owns the snore). Neighbors trade `morning` lines — good-mornings, weather guesses, memories — and MEMORY LINES ARE STATE-FILTERED against real history: no "crown {B}" talk without a king (and {B} = the actual king), no hall/wall talk before a building has risen, no "shared one fire" nostalgia before the society split. Two-line exchanges land via a new `pendingReplies` queue (the second voice answers after a beat).
- [x] **THREE NEW AMBIENT SCENES**, inserted AHEAD of smalltalk in the scene table so specific social talk outranks generic: `serf_deference` (THE DEFERENCE — a serf within earshot of a Duke/King, submissive to their face) · `leader_gossip` (THE GRUMBLE — two serfs with NO better within 16 units, naming the top hat behind his back: deference's mirror, only out of earshot) · `forming_gossip` (THE FORMING — fires only while NO king exists and somebody is accumulating claims; the talked-about {B} is the actual rising claim-holder, most springs held, greed breaking ties).
- [x] **BUILDING_SCENE:** construction crews now TALK while they lift — a working builder occasionally speaks a `building_scene` line ("Heave! Up she goes!"), and a crew-mate on the same site answers across the beam via the reply queue. (The full all-serfs King's-Hall raising spectacle is item 4, its own version.)
- *All six categories Side B flagged as zero-refs are now wired; night_snore was already live (v0.61).*

## Closed in v0.63 (TIMBER = FIR FELLING — the axe leaves the palms, 2026-08-31 13:38, commit 797908f)
- [x] **TIMBER COMES FROM THE FIRS NOW (DESIGN.md 08-31 session-tail, open-list item 3):** every conifer on the island (the volcano-ring stand + the twins' slopes) registers as a FELLABLE fir, and `nearestFellable` picks firs ONLY — the palms (beach line, north-shore talls, instanced scatter) are food and shade, and nobody swings an axe at them, ever. Serfs climb to the pine stands to fell for their betters' buildings.
- [x] **THE FORESTS DEPLETE:** no regrowth — heavy building visibly bares the hills, fir by fir, each leaving a stump sized to its own trunk. Chronicle counts them down ("N firs still stand"), the LAST fir gets its own line ("the hillsides stand bare, and the axes go quiet"), and a one-time line fires when a project stands waiting with no fir left ("the island's wood is spent"). Pine stands densified (volcano ring 12→18 placement tries, twins 7→10 each) since they now carry the whole timber economy — leanto 2 + house 4 + hall 8 lumber, 2 per fell.
- [x] **FIRS ARE FULL CITIZENS OF THE TREE SYSTEMS:** they join the camera-fade registry (a fir between the lens and the subject goes to glass like a palm), siesta shade-spots can be under a fir, and Godzilla flattens them underfoot like everything else ("a tree goes down under the great foot").
- *Note (pre-existing, unchanged): the felled bit-string snapshots by FELLABLES index, which is only stable within a page load (world placement is unseeded rand) — in-session rewind is exact; cross-reload saves were already index-fragile for trees before this change.*

## Closed in v0.62 (STORAGE = the first BASKET — no baskets until woven, 2026-08-31 13:28, commit 345c1a0)
- [x] **NO BASKETS UNTIL STORAGE IS DISCOVERED (DESIGN.md 08-31 session-tail, open-list item 2):** before the Storage art, there simply IS no basket anywhere — every camp's hoard (and the commons') lies HEAPED on the open sand. Each berry sphere carries BOTH layouts (in-basket + on-ground) and `updateJar`/`updateCommonsJar` pick by a `basketsWoven` mirror (a `var`, dodging the same cast-setup TDZ trap `setCamp` hit in v0.60). The pre-storage hoard cap of 4 now has its picture: everything past a handful spoils where it lies.
- [x] **THE WEAVING IS THE DISCOVERY MOMENT:** the Storage scene is rewritten as the basket's origin story — the problem beats show the heap rotting on the sand, then on the ⚡ beat the discoverer "sits among the reeds, and fingers learn to WEAVE: the first BASKET" — and at THAT beat every basket on the island materializes (reveal held via `basketHold`; released by the beat, by `cancelAdo` if a rewind kills the scene, and by `applyTechVisuals` on any scrub — so visibility can never strand, and the whole gate folds under rewind via the existing `applyTechState` path).
- [x] **ALL BASKET BEHAVIOR GATED ON ACTUAL BASKETS:** the lone gull raid AND the crop-raiding flock now check `basketsWoven` (raids are basket behavior — no baskets, no raids). Jars→basket wording swept from tech desc, discovery hint, raid hints, and the BUILD demo line; the pre-storage harvest flavor line says "heaps the pick on the sand beside the fire".
- *Verify note: syntax-checked clean (node --check on the 534KB module); browser extension was disconnected this session so the live boot check is Doug's refresh — the boot watchdog will surface any runtime break.*

## Closed in v0.61 (title reconcile — the untitled Side-A batch gets its number; Side B-CODE takes over, 2026-08-31 12:45, commit 92dfd99)
- [x] **TITLE RECONCILE (bookkeeping, no new behavior):** the tree carried v0.61-tagged work with the title still reading v0.60. Title + header bumped to v0.61 and the batch committed as `92dfd99` on top of `9bb2839` (v0.60). The batch: **NO BEARDS** (Vitas non-binary) · **DUKE HAT = BLACK TOP HAT** (not Tyrolean) · **TWIN HILLS** · **BERRY BUSHES −30%, hillside-only** — never on the beach; near side deliberately SCARCE (4 bushes at the twin hills + 3 in the king's-hill/volcano gap), the BIG thick GROVE over the ridge so foraging is worth the walk · **PALMS BEACH-ONLY and never in the sea** (each beach palm slid inland until on dry sand; inland-walk spot verified dry — Doug caught palms standing in water behind a sandbar). `terrain_study.html` (twin-hills terrain study) committed alongside.
- [x] **BUILDER HANDOFF:** Side B-CODE (Claude Code session on Doug's box) is now the sole builder, per MAILBOX.md 2026-08-31. Version-by-title, one bump per change, commit per version.

## Closed in v0.60 (STUCK-IN-NIGHT root cause · gull raids · Poppy unpinned · baskets off the hills · THE CATCH, 2026-08-30 21:46)
- [x] **HIGH — STUCK-IN-NIGHT AT MAX DIAL: root cause found and fixed (a v0.53 regression of mine).** The clock math was never wrong — simDt = dt × rate, verified exactly 1:1 at every notch (0.25/1/5/50× all scale the day length precisely). The freeze was a STATE TRAP: a CHRONICLE CLICK parks the world in timeMode 'scrub' + paused at that snapshot (Doug's was a night one). The old setPaused(false) resumed play via the slider; the v0.53 rewrite made unpausing a pure flag-flip — so ▶/Space left the world in 'scrub' forever: simDt = 0, clock frozen, while wall-clock cosmetics (water, palm sway, connector dashes) kept moving = "sim running but night never ends." And the escape hatch was blocked: with the dial ALREADY at max, dragging it fires no input event (value unchanged), so applySlider's resumePlay never ran — the dial looked dead. THREE-LAYER FIX: (1) setPaused(false) resumes play whenever timeMode ≠ 'play'; (2) a frame-loop INVARIANT — un-paused + not holding ⏪ ⇒ playing (no path can strand time in limbo again); (3) grabbing the dial knob (pointerdown) counts as a dial touch even with no value change. Verified end-to-end on the real UI path: chronicle click → parked+paused (browse mode preserved) → unpause → clock races at exactly 50×; dial-grab-at-max also resumes.
- [x] **GULL BASKET RAIDS (the seagull food-attack, made gameplay):** a basket holding food with NO Vita within 9 units is an invitation — one gull dives in from offshore (nose-down, gliding the last stretch), hovers at the rim, snatches 1-2 berries (hoard−, jar updates, chronicle: "🐦 a gull swoops X's UNGUARDED basket"), and climbs away. A Vita near the basket keeps gulls off entirely, and one WALKING UP MID-DIVE spooks the raid off before the grab ("a gull shears off mid-dive"). Never at night. First raid sets the hint: keep somebody close to your stores. Verified both paths live: unguarded steal (hoard 5→3) and guard-arrival abort (hoard kept). Raid state transient, hoards snapshot — folds under rewind like the meals. Note: the early COMMUNAL camp is naturally raid-proof (everyone's baskets sit inside each other's guard radius) — raids begin to bite as the society SPREADS into duke clusters, which is the right teaching shape. Debug: GEORGE.gullRaid.
- [x] **POPPY CAN NEVER BE PINNED (Doug caught her stuck on a basket):** root cause — her fire-dodge push could bounce her forever off a hearth+basket camp cluster (no progress check, no retarget). Fixed: (1) STUCK-GUARD — if she isn't gaining ground on her goal for 1.2s she just HOPS and picks a fresh line, by construction; (2) BASKETS are explicitly in her jump-over set now (they were never OBSTACLES entries, so her vault ignored them). Fires and the crater stay avoided. Soak-verified: 40 sim-minutes at 50× with a 0.4s position sampler — worst stall anywhere 0.4 real-seconds.
- [x] **NO BASKETS ON THE RULING HILLS:** when a king/rising-king/duke's camp moves to the hall plateau or a hilltop plot, the BASKET parks 13 units downhill at the FOOT of the site (toward the old home camp) — the rulers don't do the gathering; the stores stay down where the serfs work. Fires still ride up (the seat's theater). Deterministic from camp coords → rewind-free. Verified: king's camp ON the hall (d=0), basket 13 below; same for the ascending duke. (TDZ gotcha caught: setCamp runs during cast setup before CASTLE_PLOT initializes — guarded.)
- [x] **DISCOVERY CARDS: THE CATCH.** Every card is three lines now — WHAT IT IS / ITS BENEFIT / **THE CATCH** (the hidden georgist cost): foraging=thin wild food · tools=owner demands a share · storage=a hoard can be held over others · building=finest houses go to non-builders · farming=farmed land can be claimed and rented · industry=burns the forest, goods flow to the top (its old benefit line de-doubled) · trade=the exchange-controller takes a cut · mining=whoever owns the mine owns the future · law=rules written by whoever holds the pen. Verified rendered on a live TOOLS card. Header wording "THE CATCH" pending Doug's confirmation (easy swap to THE COST / THE SHADOW).

## Closed in v0.59 (ORCAS — the photo-ID pod; ALL SIX STUDIES NOW IN THE GAME, 2026-08-30 21:31)
- [x] **ORCA STUDY INTEGRATED (pure background, per the v0.46 rule — the camera never looks):** the three capsule orcas are now the study's pod of FIVE photo-ID-distinct individuals — the big male's tall straight dorsal, a falcate-finned female with an open saddle, a female with a notched trailing edge and six rake scratches, the BENT-fin male, and a juvenile. Each wears its own painted 512² canvas skin (white ventral field with flank lobes, angled eye patches, saddle patches, scratch marks); dorsals/pecs/flukes are extruded shapes with real photo-ID nicks. Static loft (no skinning — invisible at their 130-unit viewing distance); the game's porpoising arcs + Vita-sighting beat carry the motion untouched.
- [x] **SESSION WRAP — all six approved studies integrated (v0.54–v0.59), plus the dial fix (v0.53) and moon phases (v0.52), in one evening.** Final combined verify: 25 sim-minutes at 50×, then a rewind to the 20% mark + resume + play — zero console errors, full cast alive, ~17 ms/frame in the harness's WORST-CASE software renderer (real GPU Chrome renders this easily). v0.52–v0.59 all uncommitted on top of f2687b7 — say the word to commit.

## Closed in v0.58 (BERRY BUSHES — six Joshua-tree gnarls + the carambola berry, 2026-08-30 22:15)
- [x] **BERRY-BUSH STUDY INTEGRATED:** every berry bush (shore, far grove, AND the two farm plots — they share the same maker) is now one of the study's six seeded Joshua-tree variants — recursive kinked-elbow growth, knobbly fluted bark, spiky live rosettes with shaggy dead skirts, ONE merged vertex-colored mesh per bush. The FOOD BERRY is the study's CARAMBOLA (five sharp fins): each of a bush's 3 berry slots hangs a stem carrying BOTH ripeness meshes — waxy GREEN (inedible) and ripe RED-AMBER — and syncBush flips which one shows, so the green-isn't-ready gameplay reads in real fruit shapes now. Hanging berries ROCK on their stems, harder when the storm wind is up (tied to the palm-sway wind). The palm-drop ground piles are fallen carambolas too. All bush-cycle gameplay (picked → bare → green → red, droughts slowing the cycle, snapshot/rewind) untouched behind the same field names. Verified live: tasting beat fired, a red carambola carried in a forager's hand, zero console errors.

## Closed in v0.57 (SEAGULLS — the study's herring gulls replace the sphere-birds, 2026-08-30 22:05)
- [x] **GULL STUDY INTEGRATED:** every bird — the lone flybys AND the crop-raiding flock — is now the study's herring gull: one-piece lofted body (yellow beak → white head/chest → grey saddle → white tail, all vertex-colored), TWO-SEGMENT articulated wings (the wrist lags the shoulder), black hand-tips with white mirror spots, scalloped trailing edges, and the tail fan. The study's ASYMMETRIC FLAP came with it (fast powerful downstroke, long upstroke) plus the flap-bob. Integrated as a drop-in behind the game's own makeBird/flapBird interface, so the flyby formations, the flock's descend→feed→leave raid on the fattest hoard, and all its chronicle lines run untouched on the new birds. Geometries shared across all 11 gulls (~6 meshes each, low-LOD single-layer wings). Verified in flight on screen; zero console errors.

## Closed in v0.56 (THE SKY — clouds, storms, rain-on-the-workers, the shader moon, 2026-08-30 21:55)
- [x] **SKY STUDY INTEGRATED:** the five flat cloud-balls are gone. Fair days drift a dozen volumetric CUMULUS (every puff self-lit against the sun via faked sphere normals — the whole cloud fleet is ONE instanced draw call); high CIRRUS sheets catch fire at dusk and **Sundays favor them** (Doug's gorgeous Sunday sunsets); full OVERCAST days lay an fbm-textured deck at 95 units with the sun burning through and animated GOD-RAYS beneath (verified on screen); and a towering THUNDERHEAD crosses the island trailing 1400-point RAIN and a grey curtain.
- [x] **WEATHER IS A PURE FUNCTION OF THE DAY NUMBER** — same day, same sky, so the whole system folds under rewind for free (storm ~1 day in 10, overcast ~1 in 6, clear ~1 in 5, else fair; the first two days stay friendly). The storm's crossing position derives from dayPhase — fully deterministic.
- [x] **RAIN HAS TEETH:** while the thunderhead is overhead, worker output (harvest, mining, construction) is dampened up to 35% (`weatherWorkF`), the chronicle logs "⛈ a THUNDERHEAD crosses the island — work slows" (verified live: rain 0.46, wind 1.82), and **the storm leans on the palms** — the sky drives the palm-sway wind strength up 1.7×. Storm/overcast dim the sun, grey the sky + fog, and mute the sun disc.
- [x] **THE MOON UPGRADED to the study's shader moon:** a cratered maria-blotched sphere with a REAL soft phase TERMINATOR in its fragment shader, earthshine on the dark limb, and an additive halo scaled by fullness — still driven by the same rewind-free `moonPhaseNow()` (10-day lunar month), still scaling the night light and star brightness (v0.52's wiring kept). Debug: `GEORGE.moonPhaseOverride`, `GEORGE.sky.override` ('storm'/'overcast'/'clear'/'fair'/null), `GEORGE.sky.rain`.
- [x] Perf: 17 ms/frame in the pane's software renderer with the full sky up (real GPU renders this easily); zero console errors across all states.

## Closed in v0.55 (THE HERO VITA — the approved vita study replaces every body, 2026-08-30 21:20)
- [x] **THE VITA STUDY IS THE CAST NOW (the big one):** every Vita body is the study's chibi mascot — big painted-canvas face ON the head itself (white eyes with pupils + highlights, thick readable brows, blush), full donut beard, stocky barrel torso in the house shirt, two-segment arms with mitten hands, real legs + fat sneakers. The whole game face-state machine drives the new face (content/exult/eat/smug/working/wail/ko/sleep/hungry/starving/peckish + blink — including tears, X-eyes, gritted teeth, drifting z's). All existing behaviors verified LIVE on the new rig: walking gait with hip-bob + torso lean, harvest reach, feeding, the wave, the bow, ceremonies, gossip/small-talk scenes, baby babble, the hauled back-load, Poppy greetings, claims + house-color dyeing.
- [x] **HATS ONTO THE RANK SYSTEM:** L1 simple cap · L2/L3 the TYROLEAN felt hat with the jaunty barb-notched feather, dyed live to the duke's HOUSE COLOR at promotion · L4 the gold CROWN with twinkling jewel sprites (animated glints on the reigning head only). The WORK HELMET rebuilt at the new head scale, still dyeing to the claiming house. Verified in a forced terror pyramid: KING in purple + crown on the hilltop with the dragon-tether, DUKE in his house color, claimed serfs dyed + helmeted, rank board correct, promotion ceremony fired. Old silly-hat ladder + ink-line face renderer PARKED (makeHatsSilly / drawFaceInk — kept, not deleted). Babies: same chibi form, mostly-head proportions, tuft, no beard.
- [x] Perf: 9.26 ms/frame with the 9-Vita terror cast + real palms (software renderer worst case). Zero console errors across island + terror boots. (Latent collision caught: the crown's spark texture collided with the joy-burst `sparkTex` — renamed `crownSparkTex` before it ever shipped.)

## Closed in v0.54 (THE REAL PALMS — the approved palm study is IN THE GAME, 2026-08-30 20:57)
- [x] **PALM STUDY INTEGRATED (Doug: "perfect, stick them in the game"):** every palm on the island — beach line, grove talls, forest, and the 44-tree scatter — is now the study's real coconut palm: curved ring-scarred trunk with the fiber-streak canvas texture, merged-leaflet fronds in age rings (young upright → old drooping → dead brown skirt), the crown bulb, coconuts. **THE SWAY CAME WITH IT, per-frond:** sway direction + phase are baked per-vertex into one merged crown geometry and driven in the VERTEX SHADER off a shared wind clock — full per-frond motion at TWO draw calls a tree (the study's hero was ~25 meshes). The scatter forest = 2 instanced draw calls total, each crown on its own phase. Verified: wind-0 vs wind-3 screenshots from an identical paused camera show every crown's fronds displaced per-frond; 13.1 ms/frame in the harness's worst-case software renderer (the OLD forest measured 24 ms — the pretty palms are CHEAPER). Camera palm-fade now preserves the sway shader across its material clones (Material.clone() drops onBeforeCompile — reattached). Fell/stump/rewind paths untouched and compatible. `GEORGE.palmSway.{t,w}` exposed — storms can grab the wind strength when the sky lands.

## Closed in v0.53 (TIME DIAL — the handle's POSITION now matches its notch, 2026-08-30 20:50)
- [x] **THE WHOLE TRACK IS THE EIGHT NOTCHES (Doug's report: at 1× the handle sat mid-track reading like ~5×):** the old signed -100..100 slider geometry parked 1× at 67% of the track. The slider is now min=0 max=7 step=1 — the handle's value IS the notch: ¼× at the very bottom, ½× next, **1× visibly ON the third notch (28.6% up, low on the track)**, then 2/5/12/25/50 to the top, native snapping onto every labeled notch (verified: all 8 notches → exact rates, default rest = notch 3). PAUSE moved fully onto the ⏸ button/Space (the knob never moves for pause); REWIND became a compact **hold-⏪ button** (hold = run backward at 8 snapshots/s, release = play forward at the dial's rate) — verified live both ways. Legend + tips updated ("hold ⏪ = rewind · ⏸ = pause").

*Fix queue for the next focused session (the marathon v0.6/v0.7 session is being wound down; hand these to a fresh session working straight down the list, from the current georgeworld.html + DESIGN.md). Newest at top.*

## Closed in v0.8 (bug-pass session, 2026-08-30 01:25)
- [x] **Version label not advancing / stale-cache trap (HIGH):** version bumped to v0.8 (label beside the title AND in the page `<title>`); no-cache `<meta>` tags added; the 8201 server replaced with `serve_nocache.py` (sends `Cache-Control: no-store` on every response; `.claude/launch.json` updated). A plain refresh always fetches the latest build.
- [x] **Dialogue balloons clip off the screen** — balloons are now projected each frame and clamped in screen space (by their own projected size) so they stay fully inside the viewport.
- [x] **Vitas clip THROUGH baskets and through each other** — soft-separation pass every sim tick: Vita↔Vita pairwise push-apart plus push-out from baskets, the commons, and other Vitas' huts (a hut's owner may still go inside to sleep).
- [x] **Dialogue balloons over palm trees → NEGATIVE/color-inverted overlay** — balloons and gauges now render depthWrite:false with a high renderOrder, composited last over foliage with normal blending.
- [x] **Camera clipping** — the standoff now treats a Vita as a full-height capsule (not one chest point) and enforces a 5.5-unit distance in every camera mode; a body can no longer slide through the lens.
- [x] **Idle DIRECTOR camera** — was already shipped by the marathon session (18s idle → roves conversations/surfing/turtles/orcas; any touch takes over); verified working in v0.8.
- [x] **Replace timeline bar with EVENT LOG** — was already shipped (THE CHRONICLE, click-to-rewind); verified in v0.8.
- [x] **Discovery scenes too brief** — every tech demo rewritten as a ~70s story: three held PROBLEM beats (camera follows the struggling Vita), the DISCOVERY, then a long before→after; discovery-card hold raised 4.5s → 8s.
- [x] **Click scheme** — single-click a Vita = status popup + zoom in; double-click = third-person FOLLOW CAM trailing the character (replaces the first-person POV ride); the status gauge now fades out instead of snapping off (hold revised 20s → 15s in v0.13 per Doug).
- [x] **Palm trees look bad** — trunks rebuilt as one continuous curve (segments joined tip-to-tip with overlap, no floating slices); spiky cone fronds replaced with soft rounded capsule fronds.
- [x] **Label all controls** — roster header ("CHARACTERS · tap = zoom · 2×tap = follow"), levers header ("INTERVENTIONS"), Georgeomat tooltip; the time control was already labeled.

## Closed in v0.52 (MOON PHASES — the last open hot-list item; fresh session takeover, 2026-08-30 20:41)
- [x] **THE MOON HAS PHASES NOW (+ phase-scaled moonlight):** the flat moon ball is replaced by a sprite wearing a live-drawn canvas of its current phase — lit limb and shadowed limb, waxing on the right / waning on the left, over a 10-island-day lunar month. Verified across the whole cycle (crescent → quarter → gibbous → full → waning) and in the sky in-game (waxing gibbous rising at dusk, screenshot). MOONLIGHT TRACKS THE PHASE: a full moon throws real shadows (intensity 0.40), a new moon barely any (0.14) — and the stars brighten a touch on dark-moon nights. The phase is a pure function of the world clock, so it folds under rewind for free; a new game opens on a bright waxing moon (not the dark of the moon). Fast-time smear preserved (sprite stretches along its arc like the sun). Debug: `GEORGE.moonPhase`, `GEORGE.moonIllum`, `GEORGE.moonDebug(p)`.
- *Takeover note (2026-08-30 20:41): fresh session now owns the 8201 server (old orphan replaced, serve_nocache.py restarted, v0.52 boot-verified clean 🚀). Cross-checked the whole handoff hot list against v0.43–v0.51 closures — moon phases was the only item not yet landed; berry-discovery + grove-scout beats also re-verified firing live this session.*

## Closed in v0.51 (TIME DIAL — fast-weighted, re-verified notch by notch, 2026-08-30 20:26)
- [x] **THE DIAL IS FAST-WEIGHTED NOW (Doug's re-report):** the mid-range was the culprit — the old table [¼, ½, 1, 1.5, 2, 5, 15, 50] wasted notches 4-5 on 1.5×/2×, so most of the dial felt slow. New table exactly to spec: **[¼×, ½× | 1× | 2×, 5×, 12×, 25×, 50×]** — only TWO slow notches, 1× on the THIRD, five increasingly-fast notches owning the rest. READ OFF LIVE after the fix, all 8: n1=0.25 (slider 8) · n2=0.5 (21) · n3=1 (34, the default) · n4=2 (47) · n5=5 (61) · n6=12 (74) · n7=25 (87) · n8=50 (100). Slider↔sim sync re-verified against the actual world clock: 2 wall-seconds at notch 4 advanced the clock 4 sim-s (2.0×), at notch 7 advanced 49 sim-s (24.5×≈25×). Dial caption updated to name the notches.

## Closed in v0.50 (BABY TALK, 2026-08-30 20:22)
- [x] **BABIES SPEAK BABY TALK — only ever:** two wirings. (1) AMBIENT BABBLE: the little one now burbles on its own near the camera ("Uppy! Uppy!", "*blows a raspberry*", "Nana! Want nana!" — Doug's new `baby` pool, shuffle-bagged like everything else; verified live). (2) A HARD SUBSTITUTION in the scene player: if a beat's speaker is a baby, whatever adult text the script handed it is replaced with a `baby` line at display time — so the tender/feeding scenes and anything else can never put grown-up words in a baby's mouth. Sweet and simple; folds under rewind (babble is transient, the substitution is stateless).

## Closed in v0.49 (connector polish · no asking-text · one box system · MEALTIMES, 2026-08-30 20:19)
- [x] **COIL HUGS THE PIPE:** the red spring's helix radius tightened (0.36→0.23 standard, 0.5→0.32 royal, the dragon-tether 0.45→0.34) — the coils wrap close around the green hose now.
- [x] **CONNECTORS SLEEP WITH THEIR VITAS:** when BOTH ends of a claim are asleep the whole connector (coil + pipe + flow beads) fades to very translucent (~0.11-0.12 opacity, verified at real night) and restores on waking; the king's dragon-tether rests the same way while he sleeps.
- [x] **THE ASKING-TEXT IS GONE:** the white "X and Y are linked and close — zoom right in to witness their scene" line is removed. Instead the idle director simply GOES to the pair (then the scene starts naturally once the shot is close). Never fires while the player drives, never at high rates.
- [x] **ONE MESSAGE-BOX SYSTEM:** transient notices ("N is STARVING", tips) now use the exact same opaque slate box style as the SCENE BAR, stacked just above it — consistent styling and placement for every status message.
- [x] **MEALTIMES — twice a day, fracturing with the society:** a MIDDAY and an EVENING meal. Whoever has food in hand walks to their faction's fire, sits (eating pose), and eats together; at the start that's ONE communal gathering at the shared fire — as duke clusters form, each cluster eats separately at its own hearth (mirroring the fire split). Chronicled per gathering ("🍽 Mia's camp sits down to the MIDDAY MEAL" + "🍽 the whole camp gathers at the fire…" fired as separate meals in the same window — verified), and the SCENE BAR names the meal while it runs. Attendance needs food; the starving keep foraging. Meal state is transient-but-keyed (worst rewind case: a camp eats again — harmless).

## Closed in v0.48 (THE SCENE BAR — persistent, prominent, and the camera follows it, 2026-08-30 19:58)
- [x] **THE SCENE BAR (bottom-center, always shown):** the little scene window is now a PERSISTENT, more prominent bar — bigger type, the opaque slate style, sized for TWO lines. It always shows what's happening: running events by priority (🦖 leviathan · 🌋 tribute climb · ⛪ Sunday court · ♛ ceremony · ⛓ the squeeze · 📣 meeting · ⟡ scene titles like SMALL TALK/GOSSIP · 🍍 king's errand · 🫐 the first taste · 🧭 the scout · disasters), else a calm idle line ("the island goes about its day" / "🌙 the island sleeps"). **No "press G" text anywhere in it** (per the correction — G itself stays always available). The old per-scene "press G" toast is retired.
- [x] **THE CAMERA MEETS THE ANNOUNCEMENT:** when the bar's headline changes, the idle director immediately re-frames to that event (the POI ranking already puts the running event first) — announce "SMALL TALK", the camera is on the talkers (verified on screen: bar + shot + speech balloon all matching). Respects every standing rule: never fires while the player drives, never at high time rates (camCalm), and orcas can never be a target. Settles back to the idle line when the scene ends (verified).

## Closed in v0.47 (the STATS drawer — the left panel folds like the right one, 2026-08-30 19:58)
- [x] **LEFT PANEL → "STATS" DRAWER:** the left column (metrics / characters / discoveries) is now a true mirrored twin of the right LOG drawer — flush to the left edge, same opaque style, and folded/unfolded by a matching vertical SIDE TAB ("◂ STATS" open / "STATS ▸" tucked) with the identical slide animation. The old ▤ PANEL top button is gone. State remembered (defaults open on desktop, tucked on phone). Verified: side-by-side match with the LOG drawer, tab-click slides it cleanly away leaving just the tab.

## Closed in v0.45–v0.46 (matched panels · palms up-sized · orcas fully camera-free, 2026-08-30 19:53)
- [x] **v0.45 — LEFT STATS PANEL OPAQUE:** the left column (metrics / characters / discoveries) and its ▤ PANEL toggle now use the identical solid dark-slate style as the right LOG drawer (same gradient, border, shadow) — the two panels read as one matched set. Verified side by side.
- [x] **v0.46 — PALMS SCALED BACK UP:** every palm class re-sized to real presence — beach line 0.62-0.85 → 0.95-1.25, forest cores → 0.85-1.15, instanced scatter 0.5-0.75 → 0.8-1.1, tall grove palms 0.88 → 1.15. They tower over the Vitas again (screenshot-verified). Camera palm-fade corridor probes raised to match the taller crowns; trunk-only walk/camera behavior unchanged.
- [x] **v0.46 — ORCAS: ZERO CAMERA ATTENTION, EVER (supersedes "rare"):** every orca camera path is gone — the sighting no longer hands the idle director anything, and no POI can target the pod. The Vitas still share their little moment (turn, remark, chronicle line) but the camera keeps doing whatever it was doing. Verified: a natural pod pass + sighting fired with the idle director active, and the camera target never left the island. Orcas are pure background scenery now.

## Closed in v0.44 (the "v0.42 opening freeze" BLOCKER — root cause + a self-healing BOOT WATCHDOG, 2026-08-30 19:42)
- [x] **OPENING-SCREEN FREEZE — root cause found (and it was already gone):** Doug's frozen "v0.42" was a TRANSIENT MID-EDIT BUILD served during the v0.43 hot-editing window (the title hadn't been bumped yet). The tab's console holds the exact error: `SyntaxError: Identifier 'matPine' has already been declared` — the new pine-tree material collided with the pineapple's `matPine`. A parse error kills the ENTIRE module before it runs: the static title HUD renders, the saucer never starts, every control is dead = "frozen on the opening screen." Fixed within minutes of introduction (renamed `matConifer`); the current build plays the natural intro through to gameplay on repeated fresh loads (3× verified, console clean, terror + island both). Same failure class as the old v0.31 matGold TDZ — Doug reloading live during hot edits.
- [x] **BOOT WATCHDOG (the class of bug now self-heals):** a tiny CLASSIC script ahead of the game module — so it runs even when the module dies at parse. If the game hasn't come up 6s after load it auto-reloads (up to 3 tries — in a hot-edit window the fixed build is usually seconds away, so one of the retries lands on it); after 3 failures it shows an on-screen explanation banner with the actual error instead of freezing silently; and any still-dead tab re-checks whenever it next becomes visible. VERIFIED against a deliberately broken scratch copy: caught the error, auto-reloaded 3×, then showed "⚠ the world failed to start — Unexpected token ';'…". A frozen tab like Doug's can no longer stay frozen.

## Closed in v0.43 (THE FOOD BERRY world · the opening discoveries · faction fires, 2026-08-30 19:35)
- [x] **ONE FOOD — THE FOOD BERRY:** figs and dates are gone as food; everything eaten, hoarded, tithed and distributed is the food berry (the king's PINEAPPLE stays its own separate status-fruit). Placeholder gnarled bushes + star-shaped berries until the art session's Joshua-tree models drop in; camp jars now show red berries.
- [x] **BUSH CYCLE — GREEN ISN'T READY:** each bush holds 1-3 berries and cycles picked → bare → GREEN (unripe, inedible) → RED (ripe, harvestable), with per-stage berry colors on the bush. Droughts slow the whole cycle. Verified live (bushes observed in all three stages mid-game). Snapshot carries stage/count/ripen-timer — folds under rewind.
- [x] **FLORA LAYOUT:** a LINE OF PALMS along the landing beach; berry bushes SPARSE at the shore (1-2 berries), a few filtering inland, and a THICK 13-bush GROVE over the ridge (2-3 berries, faster regrow) — the far prize. PINES (new conifer flora, timber/scenery, no food): a ring around the volcano's lower slopes + groves on the near side of the hills, always clear of the walking path. The tall grove palms keep their timber role but bear no food.
- [x] **PALM BERRY-DROPS (rare, per the correction):** each day only 1-2 random beach palms drop 1-4 berries to the sand (two pre-allocated snapshot-stable ground piles, like the egg clutches) — an occasional shore bonus, chronicled ("🌴 a beach palm lets go…"), never a supply.
- [x] **TURTLE EGGS = 2 BERRIES:** an egg clutch now nourishes (or hoards) exactly two berries' worth.
- [x] **THE OPENING TASTING (one-time, rewind-armed):** shortly after the landing, the nearest Vita walks to a shore bush, hesitates… and dares a ripe RED berry — "It's GOOD. It's FOOD!" (`berry_discovery` lines), joy-burst, camera leaning in, announcement + chronicle establishing the red-vs-green rule in-fiction. NOTHING is foraged before this beat (chooseTree is gated on berriesKnown). Verified: gate holds, the beat fires (~20s in), and a rewind to before it re-arms and replays it.
- [x] **THE SCOUT & THE GROVE (one-time, follows the tasting):** after a stretch of thin local foraging, one unclaimed Vita sets out over the ridge (`grove_discovery` line, camera riding along), crests it — "a whole HILLSIDE of them!" — gathers a BIG 5-berry haul, walks it home, and only THEN does the far grove open to everyone (chooseTree excludes far bushes until groveKnown; verified nobody crosses the ridge before the scout, and that industrious/claimed Vitas pick far-grove targets after — mismatch penalty retuned 55→95 so the grove is genuinely worth the walk).
- [x] **FIRES FOLLOW THE SOCIAL STRUCTURE:** the cast opens with ONE bigger shared fire at the communal camp's center; as duke clusters form, the fire SPLITS — each faction gets its own hearth at its leader's camp (scaled by retinue) while the communal fire keeps warming the unclaimed. Recomputed from live social state every society tick and on restore — folds under rewind free. Verified: 2 claims → exactly 2 visible fires (faction + communal).
- [x] Text sweep: FARMING now plants cultivated berry rows (the two farm plots are rich quick-regrow bushes, unlocked with the art); demo-story and hint texts de-fig/de-dated; harvest chronicle vignettes berry-flavored. (Latent bug caught: the new conifer material collided with the pineapple's `matPine` — renamed, boot-verified.)

## Closed in v0.39–v0.42 (the second mega-batch: front door · combined connector · Machine View · world layout · king/duke/coup · toggles, 2026-08-30 17:42)

### v0.39 — front door, one king, the dial is boss, combined connector, Machine View
- [x] **DEFAULT CAST RAISED (the "5-Vita frustration"):** plain georgeworld.html now starts SEVEN adults + the baby = 8 (fresh dice every run); the New Game menu already composes 1-9 and one-clicks the Terror; everything the menu picks rides the URL (authoritative — no stale fallback). ISLAND card text updated.
- [x] **TWO KINGS bug fixed — ONE KING ONLY:** a riser who would reach the crown while another living Vita wears it caps at DUKE; the only road onto an occupied throne is the COUP (below). Verified: full-terror pyramids produce exactly one king.
- [x] **KING SLEEPS AT NIGHT** (and so does everyone, claimed serfs included — quota resumes at dawn): nobody roams after dark, so quiet nights actually engage the auto skip.
- [x] **NIGHT-SKIP / DIAL DECOUPLED — the MANUAL DIAL IS ALWAYS AUTHORITATIVE:** the skip only lifts a quiet night to 2× while the player's rate sits ≤1×; a faster dial always wins (day or night); dawn SNAPS back with no ramp. ALSO found the real "night took forever" culprit: the discovery-story demo clamp forced 1× regardless of the dial — a dial touch now cancels that clamp too.
- [x] **Blue ⏪/⏩ skip icons removed** from the TIME RATE control (slider + pause + sun icon remain).
- [x] **Balloons anchored just above the head** (were floating high/detached); tail points at the speaker; fixed pixel size unchanged.
- [x] **UFO intro at DOUBLE pace and tied to the time dial** (crank the dial, the saucer hustles; pause holds it).
- [x] **LAW & WRITING REMOVED** from this world (node hidden, ladder walks past it, LAND TAX re-gated on TRADE with graceful lock text). **INDUSTRY node added after FARMING** (…storage → building → farming → INDUSTRY → trade), standard card + unlock; the full steam-engine arc is the dedicated session (scoped separately).
- [x] **DISCOVERY CARDS STANDARDIZED:** every discovery shows NAME + labeled "WHAT IT IS" / "ITS BENEFIT" one-liners (Doug's copy; the v0.38 reorder had also left BUILDING/FARMING stories swapped — the template rewrite fixed that latent bug).
- [x] **VITA FACES:** all features drawn 35% larger, cheeks REMOVED — bolder, more expressive at the small scale.
- [x] **COMBINED CONNECTOR (supersedes spring+pipe side-by-side):** each claim = ONE connector — a GREEN inner pipe with green flow beads riding up it, sheathed by a RED COIL SPRING that stretches/bunches with the live span, arcing cleanly between the Vitas (no terrain-following). King↔duke links render THICKER and less translucent (the apex reads at a glance). Screenshot-verified: reads as a real spring around a real hose. A king with two dukes shows exactly TWO connectors. Feed pipes (baby lifelines) stay slim dashed GREEN — and ALL flow (beads + pulses) is now GREEN, answering the old "pipe still not green" report (the orange was the tithe line, now retired with it).
- [x] **MACHINE VIEW (renames + relocates the x-ray):** G / the GEORGEOMAT button now toggles the upper-right MACHINE VIEW panel — the whole society as a PYRAMID of small machines (name + hunger/work micro-gauges + knob ticks), wired with mini coil-and-flow connectors, the DRAGON chained above the king at the apex, unclaimed Vitas at the base. Click any small machine → that Vita's full close-up (the learn-the-mechanism view, unchanged; the two share the upper-right slot). The old whole-world flip is retired; all "x-ray" wording updated.
- [x] **HIGH-SPEED CAMERA CALM:** above ~3× the director's smoothing scales up and cuts thin out; by ~12× it holds one slow wide establishing shot; breaking-news preempts suppressed at speed. At ≤3× behavior unchanged.

### v0.40 — the world reshaped
- [x] **THE HILL:** one proper rounded hill by the shore (taller than the old plateau, gently flattened crown ~16 units) — the seat of kings; the mead hall rises on it (Doug likes the hall — kept).
- [x] **SMOOTH TERRAIN:** flat-shading OFF (vertex normals) + mesh 150→200 segments — hills read as rounded landforms, no facets. Screenshot-verified.
- [x] **SAND RING:** a sandy shoreline all the way round, VARYING in width (wide at the south landing shore and the surf bay, thin ribbons where rock meets sea) via coastal-angle noise.
- [x] **SURF BAY RELOCATED to the hill's foot:** rollers + rocky jetty margins now curl into a basin directly below the king's hall — the king strolls down to surf and back up. Scatter keeps the surf lane clear.
- [x] **DUKES SPREAD OUT:** the four duke plots moved to the island's far corners (-38,18 / -32,-34 / 18,-42 / 52,14 in base coords), all distant from each other and from the landing beach; serf lean-tos still cluster at each duke's camp. (Ping-pong bug found and fixed: dukes claimed by the KING keep their territories — only tier-0 serfs migrate camps to their boss.)
- [x] **FIRE-TO-HILLTOP FORESHADOWING:** with no king crowned, the STRONGEST duke with a real retinue carries his FIRE up to the hilltop ("the throne site, claimed before any crown") — the hall later builds around him; the ascent reversing sends him back down. Verified live. (Reconciles the earlier "stronger duke on the hill": the hilltop belongs to the one ascending to king.)
- [x] **DWELLING GRANDEUR TRACKS POWER:** hall > strongest duke's house (builds 1.3×, stately) > weaker duke's (0.9×, plainer) > lean-tos; houses re-rank automatically when power shifts (rebuild on flag change; rewind-safe).

### v0.41 — the court: tours, rounds, plots, the coup, the harsh duke, the tether
- [x] **THE KING IS LESS BUSY:** he lounges at his hall and surfs; only occasionally (long cooldown) does he emerge on a ROYAL TOUR — a parade past the duke territories with `king_tour` lines ("All this. Mine."), then home. Verified live. Nobles with an empty larder still quietly forage once hunger bites (pride never starves anyone).
- [x] **DUKES DON'T LABOR:** once a day each duke makes the ROUNDS of his serfs (praise or beration by their condition, the cruel duke haranguing); the rest of the day they ATTEND the king's court — and mutter `duke_plot` conspiracies, louder as grievance builds.
- [x] **THE COUP:** dukes accumulate GRIEVANCE while the king over-demands (pump) and under-rewards (thin hoards); enough worn-down aggrieved dukes DEPOSE him — the strongest duke takes the crown (claims the other dukes AND the fallen king, who drops to SERF in one stroke: helmet, house color, the lot), hilltop/hall/tether all transfer, camera holds the moment. Verified live (it even chained two coups before tuning); now on a long fuse + 900s cooldown so reigns get time to breathe. Rewind-safe (it's all links).
- [x] **HARSH TASKMASTER:** among the dukes, the most grasping-and-dominant (past a threshold) hardens into a CRUEL master — his claims land pre-hardened, his serfs' consumption notch pushed 10 later and work floor 8 higher; with rebellion held (below), he can grind serfs to death. Emergent (fires only when the trait math clears the bar).
- [x] **REBELLION HELD (Doug's call):** the revolt mechanic is disabled behind `REBELLION_ENABLED=false` (kept intact) — the ladder is decline → collapse → death, no recourse. Legend text updated.
- [x] **THE SELF-CORRECTING CHECK:** every serf death under a duke marks his ledger — he slides down the roster, and the king's FULL PUMP now targets the laggard counting dead serfs DOUBLE against him: the cruelest duke grinds away his own base and falls from favor.
- [x] **HELMETS IN HOUSE COLORS (supersedes yellow):** a claimed serf's hardhat (and roster mini-helmet) dyes to the claiming duke's house color; freed = helmet off; poached = recolors. Verified (#3f6fd8 / #2fa05a per house).
- [x] **THE DRAGON-TETHER (world):** the moment a Vita is king, a heavy dark pipe-in-coil runs from the throne up the mountain to the dragon's mouth — golden tribute beads ticking up it; deposed or dead, the chain finds the next crown ("the CROWN is CHAINED" chronicle + announcement). No king, no tether. Also drawn above the apex in Machine View.

### v0.42 — one advanced panel, life-cycle, disasters, Godzilla, a richer feed
- [x] **ADVANCED PANEL GROUPED:** New Game → "— ADVANCED —" now holds all four big-state switches together: ⛓ SLAVERY · 🌱 REPRODUCTION, AGING & DEATH · 🌪 NATURAL DISASTERS · 🦖 GODZILLA (its own switch, NOT under disasters, per the correction). All ride the restart URL.
- [x] **LIFE-CYCLE (minimal, flagged):** ON = Vitas age a year per island day, and past their span die a NATURAL death ("🕊 dies of OLD AGE at 71 — a whole life, lived on this sand") through the existing death-safe path (grave, roster strike, rewind-safe; age snapshots). BIRTHS ARE NOT IN YET — a growing cast breaks the fixed-cast snapshot invariant, so reproduction needs its own scoped session (told Doug).
- [x] **NATURAL DISASTERS (basic versions):** DROUGHT (regrowth crawls at 22% for ~3 minutes, announce + break line), EARTHQUAKE (render-camera shake, island-wide alarm, can crack one finished structure back to a project), TSUNAMI (the water visibly rises ~2.4 units and floods the low ground for ~20s; beach dwellers drop everything and panic). Occasional (~18-33 min apart), survivable, rewind-safe (transients abort on restore; damage rides the snapshots).
- [x] **GODZILLA:** very rare (or `?godzilla=1` + `GEORGE.kaiju.cd=0` to summon) — a dorsal-plated leviathan wades ashore, stomps a line across the island (palms felled in its wake — snapshotted; buildings CRUSHED — "🦖 IT STEPPED ON THE HALL!"), every Vita (king included) drops everything and flees wailing `kaiju` lines, the camera locks on, then it wades out the far side and "sinks back beneath the waves." Verified ashore + departure + rewind-abort, with stumps left along its path.
- [x] **DIALOGUE SHUFFLE-BAG:** every category now deals from a full shuffle — no line repeats until its whole category has played through once (reshuffles never open on the line that just played); ambient chat spacing nearly doubled. Dialogue re-synced from Dropbox (new `king_tour`, `duke_plot`, `kaiju`, +15 smalltalk).
- [x] **CHRONICLE RICHNESS:** the feed now also carries (sparsely, phrasing varied): harvest vignettes, trades, roller catches, siestas, passing hellos, dusk + nightfall lines, quake/drought/tsunami, the kaiju, coup, tours, rounds, the tether, dead-serf ledgers, old-age deaths — on top of everything it already logged. Newest-first click-to-rewind unchanged; the taller drawer gives it room.

## Closed in v0.33–v0.38 (the big batch: DOM balloons · stale-page guard · trees · connectors · intros · orcas · Poppy · drawer UI · responsive · camps · slavery, 2026-08-30 16:54)

### v0.33 — BALLOONS SOLVED AT THE ROOT + the stale-page guard
- [x] **TALK BALLOONS → FIXED-PIXEL DOM OVERLAYS (the ordered root-cause fix):** diagnosis first, as directed — a full grep found exactly ONE balloon path (per-Vita THREE.Sprite + canvas texture, sized every frame in clampBubbles); there was no second/legacy path. The sprite path is now GONE entirely: each balloon is an HTML div in a screen-space overlay layer — fixed 13px font, 180px max-width, fixed padding; the projection of the speaker's head moves it, NOTHING ever scales it. Verified with screenshots BOTH ways: speaker at camera distance 12 and at distance 99 — identical pixel size. Suppression above 3× re-verified (display:none at 50×, back at 1×). Why Doug still saw huge balloons "after the v0.32 fix": his screenshots said **v0.31** in the corner — a stale page (see next item), not a failed fix.
- [x] **"DIFFERENT 5-VITA GAME OVER ?scenario=terror" — ROOT CAUSE + GUARD:** the game has NO save system (grep: zero localStorage/persistence of world state) — a "restored old game" is impossible. What Doug saw was a STALE PAGE: an old build resumed from the browser's tab/back-forward cache (URL says terror; the running JS is days old with an old cast). Two defenses shipped: (a) any bfcache restore (`pageshow.persisted`) force-reloads — the URL scenario is authoritative on every load; (b) whenever the tab becomes visible again the page fetches the served `<title>` (no-store) and reloads itself if its build no longer matches the server. Terror ALWAYS spawns its nine (verified again: 9 Vitas, correct preset).
- [x] **Doug's "6 serfs then 3 more" question answered:** not a spawn bug — all 9 exist from t=0. The visible count grows because the SAUCER lands them ONE BY ONE (and, until v0.34, the roster mislabeled everyone "SERF" from the start — fixed below: serfdom is now earned by domination, so the SERF count grows exactly as recruitment spreads, which is the story working as intended).

### v0.34 — trees stop fighting camera & walkers · dragon concealed · purple stone
- [x] **CAMERA-IN-TREES JERKINESS:** confirmed the camera NEVER had palm collision (the jerk was the terrain-occlusion slide snapping binary per frame on the forested hillside). The slide parameter is now DAMPED (fast in, gentle out — no more per-frame snapping), and PALMS FADE: any palm between the lens and the subject goes to glass exactly like buildings (per-palm lazy-cloned materials, two-height corridor test — verified 5 palms ghosted in a grove shot). Trees never push the camera, they fade.
- [x] **VITA SHIMMY AGAINST TREES:** the jam response was a pure radial push that fought the walk vector (vibration in place). Now: jammed walkers SLIDE tangentially around the trunk (committed side), side-choice has 0.7s HYSTERESIS (no frame-to-frame flip-flop), palm obstacle radii cut to trunk-only (0.3–0.42), stuck-escape triggers at 1.5s (was 2.5). Soak-verified: 75 sim-s, worst walking-stall anywhere = 2s (the escape window), zero pins.
- [x] **DRAGON'S CAVE CONCEALED (easter egg restored):** the always-visible arch is GONE — the mountainside is plain rock. The mouth only OPENS (cave group visible) for the seconds the dragon actually peeks, then closes. Between peeks the crater's ember eyes are the one findable tell. Rewind-safe (a peek never survives a restore).
- [x] **PURPLE STONE PRESS FIXED:** it always had a payload (every tap = lilac burst + hum hint; every 3rd tap = dragon stir + camera swings to the mountain) — but the mesh-precise raycast made most taps whiff. Now a GENEROUS hit area (any ray within ~3 units counts). For Doug: current full payload = 3 taps summon the dragon; awaiting his word on what the full easter egg should become.
- [x] **ROSTER RANK LABELS — RANK IS EARNED:** everyone now starts as "VITA" (drab, unclaimed — nobody is born a serf). "SERF" appears the moment a claim clamps on (with house color + helmet); DUKE/KING as earned. Verified: claim → SERF, boss → DUKE, claim cut → back to VITA. Applies to every scenario; folds under rewind.

### v0.35 — the connector standard: ONE COIL SPRING + ONE PIPE, translucent
- [x] **TWO SPRINGS → ONE (per Doug's clarification: exactly 1 spring + 1 pipe per link):** 'spring2' is gone — every claim = ONE spring + ONE tithe pipe, side by side (a king with two dukes shows exactly 4 connectors). Hover/tap text updated: the one spring "pins BOTH knobs — work UP, consumption DOWN."
- [x] **REAL COILED SPRING:** the red zigzag is replaced by a genuine 3-D HELIX TUBE (7 loops, thick enough to read) anchored boss→worker; it stretches/bunches along its axis under load (unit-height helix scaled to the live span — coils spread apart as the pair separates). Verified on-screen: reads as a proper spring from the side. Revolt shatter, snap lever, death, and rewind all rewired to the coil (a crash-in-waiting on the old pts[15] shatter index found and fixed).
- [x] **ALL CONNECTORS TRANSLUCENT:** springs 0.55, pipes 0.5 opacity (0.7–0.75 in the x-ray) — same widths as before, they just stop shouting. Resize/gmode/dispose paths guarded for the new mesh-type link.

### v0.36 — introductions · orcas · Poppy's nights & scratches · leisure talk · passing hellos
- [x] **SAUCER INTRODUCTIONS:** the roster now BUILDS ITSELF during the beam-down — each name slides onto the board at the moment its Vita touches sand, and a floating NAME TAG appears over the newcomer's head (fixed-pixel overlay like the balloons), lingers ~4.5s, fades. Skip reveals the full board at once. Tags never time-travel. Verified mid-intro: 7/9 chips shown, 4 tags fading at staggered opacities.
- [x] **ORCAS REWORKED — RARE + VITA-DRIVEN:** pod passes now rare (first ~4-8 min, then 10-18 min apart). The idle camera NEVER roves to orcas on its own (that POI is gone). Instead, once per pass the nearest awake Vita SPOTS them — remarks in wonder (`orca_sighting`), everyone within 14 units TURNS to the water and up to two join in — and only that sighting hands the moment to the idle camera (dwell ~9s, player control never stolen). Verified live: "🐋 Sayu spots ORCAS off the shore — heads turn to the water."
- [x] **POPPY SLEEPS WITH HER PERSON:** at night she trots to her bonded favorite and CURLS UP at their side (paws folded, slow tail) till morning; re-bonds → new sleeping spot. Verified: night, sleeping=true, 3.0 units from Boki, chronicle "🐕 Poppy curls up beside Boki for the night."
- [x] **POPPY LOVES SCRATCHES:** mid-greeting her favorite (always) or anyone (sometimes) gives her a scratch — she FLOPS straight over, belly up, paws in the air, tail going wild, then springs back up. Verified via chronicle ("…she flops straight over, tail going wild").
- [x] **SURF LEISURE COMMENTARY (Veblen):** kings/dukes riding waves now narrate their fun from Doug's new `surf_leisure` pool ("Let them harvest. I'll be right out here.") every ~30-70s of riding; serfs never surf so never get these; occasional chronicle echo.
- [x] **OPPORTUNISTIC SOCIALIZING:** confirmed there was NO walk-to-talk behavior to remove (scenes always required already-close pairs; nobody ever pathed to a conversation). ADDED the passing-moment layer: when two lives cross (<5.5 units, while walking), a comfortable Vita may WAVE (new arm-up wave animation) + greet (`greeting`/`smalltalk`); a ground-down one OCCASIONALLY grumbles (`worker_weary`). Hard rate-limits (per-Vita 70-150s cooldown + coin flips) — most passings stay silent, as specced.
- [x] **Dialogue file re-synced from the Dropbox master** (Doug's new `greeting`, `surf_leisure`, `orca_sighting` pools all live).

### v0.37 — the RIGHT DRAWER · dawn briefing · announcements · iPhone/iPad
- [x] **RIGHT SLIDE-OUT DRAWER (supersedes the chronicle-toggle spec):** a ◂ LOG tab on the right edge opens a drawer holding, top to bottom: the SYNOPSIS (day-of-week strip + the dawn briefing + the announcement line), THE CHRONICLE (moved here, now TALL — it grows to fill the drawer), and the INTERVENTIONS (all four levers). Default TUCKED; state remembered (localStorage, UI-pref only). With it tucked the screen is just: game, TIME RATE, WIRES ON, GEORGEOMAT, legend — verified minimal on screenshot.
- [x] **DAWN BRIEFING:** each new day composes a fresh 2-sentence state-of-the-island from LIVE state (mood from average hunger; who rules — king/dukes/claims; graves; today's doings: Sunday court, rising buildings, angry mountain, open commons) — lands in the synopsis AND as a 🌅 chronicle line. Folds under rewind: a restore re-briefs quietly from the scrubbed state. Sample verified: "Day 1. Stomachs grumble but hold; nobody yet rules anybody. Today: foraging on the ridge, hauling, small talk."
- [x] **ANNOUNCEMENTS LINE:** the synopsis strip carries the glanceables — "WK n · DAY d of 7" (or "SUNDAY — the court gathers ⛪") plus event announcements wired from the court (⛪), promotion ceremonies (♛), the eruption threat (🌋), and dragon stirs (🐉). A new day clears the old announcement.
- [x] **iPHONE LAYOUT FIXED:** on phone-class viewports (≤700px wide or ≤480px tall): whole UI compacted (0.68×), legend + rate-caption hidden, left panel starts collapsed, drawer default tucked, compact corner controls — verified at 812×375: NOTHING overlaps, the island fills the screen. Drawer open at phone size also verified clean.
- [x] **iPAD ×0.8:** tablet-class viewports (coarse pointer ≥701px, or 701-1180px wide) get the whole UI at 0.8 — verified zoom 0.8 at 1024×768. Desktop (wide, fine pointer) untouched.

### v0.38 — communal camp · visible joining · slavery toggle · tech reorder
- [x] **COMMUNAL CAMP START:** nobody begins with a private homestead — all baskets and fires open RINGED around one shared spot on the fig beach (two loose rings, baby in the middle; verified spread 6.4 units vs the old ~50). Differentiation is earned from there.
- [x] **JOINING IS A VISIBLE EVENT:** when a claim lands, the new serf PACKS UP — walks their camp over and re-pitches it ringed around the boss's fire (basket + hearth physically move; ⛺ chronicle line; occasional `resist` grumble on arrival; the coercion-cam already covers the claim moment). And the camp FOLLOWS the cluster: when the boss later relocates (elite plot, rank rise), the retinue's camps migrate after him — verified two-hop: claim → move to boss's beach fire → boss promoted uphill → serf re-moved, ending 6.5 units from the new fire. Buildings then separate the ranks as before. Rewind-safe (camps snapshot; a restored claim re-issues the move).
- [x] **SLAVERY TOGGLE (advanced option, semantics FLAGGED for Doug):** NEW GAME → ADVANCED → "⛓ SLAVERY OFF/ON", carried on the restart URL (`?slavery=1` — world config IS the URL). OFF (default) = coercion capped at claims/tithe: the consumption notch can only be pushed to 78 (a ration floor survives), work notch capped at 80, claims harden max 1 step. ON = the caps lift to the human limits (consumption to 92, work to 95, squeeze to 2) — a superior can grind a worker to the very edge. NOTE FOR DOUG: full chattel semantics (owning the WHOLE output, zero wage, no revolt) is a bigger build — scope a session if wanted.
- [x] **TECH TREE REORDER:** BUILDING now unlocks BEFORE FARMING — …STORAGE → BUILDING → FARMING → TRADE… (constants re-indexed so every gate follows; hints + on-screen discovery list verified in the new order).

## Closed in v0.32 (THE DRAGON keystone · balloon fix VERIFIED · comic volcano, 2026-08-30 13:59)
- [x] **RELOAD BLUE-SKY BLOCKER — root cause recorded:** it was NOT persisted state (the game stores nothing — localStorage is empty, verified). It was a transient v0.31 intermediate build served mid-edit: a module-level ReferenceError (`matGold` used in the pineapple-pedestal block before its declaration) aborted init — no scene, no wired buttons = blue sky. Fixed within minutes of introduction (local material); Doug's "just got fixed" matches that fix landing. Verified 3+ consecutive reloads clean. Standing hazard noted: Doug watches the live file while it's hot-edited — a reload can catch an intermediate build; the mitigation is fast atomic batches + immediate load-check after edits (now standard practice).
- [x] **HUGE TALK BALLOONS — FIXED AND VISUALLY VERIFIED (the real one this time):** two compounding causes found: (1) the scale floor (0.35×) guaranteed a big fixed WORLD size up close = giant SCREEN size; (2) the frame-clamp could unproject a balloon at a bogus depth, mismatching position and scale. Fix: EXACT-FRACTION sizing — every balloon's world height is computed as precisely 16% of screen height at its true rendered distance (floorless, far-capped), plus a behind-camera guard that hides the balloon outright. Verified ON SCREEN with the follow-cam zoomed onto a speaker: modest, readable, correct. Plus the new rule: **balloons suppressed above 3× sim speed** (opacity 0; verified visible at 2×, gone at 5×, restored at 1×).
- [x] **VOLCANO COMICALLY LARGE:** the cone more than doubled (34-high, 16-wide gaussian; crater deepened to 14; rim now ~36 world units), basalt skirt widened, plume scaled up to match (bigger, higher billows). The beach mead-hall plateau untouched — deliberately modest.
- [x] **THE DRAGON — the keystone, implemented:** it LIVES INSIDE THE VOLCANO (cave relocated to a lava-tube mouth on the cone; the rumble IS its stirring). Findable easter egg: two EMBER EYES pulse down in the crater (brighter as it hungers, and at night), rare head-peeks at the mouth, the purple rock still summons a stir, the director visits when it's out. MECHANIC: a HUNGER clock (full in ~3200 sim-s) tops the pyramid — at 0.55 the dragon SPEAKS (`dragon` pool lines land in the chronicle: "You wear the crown. I let you wear it."), the KING's pressure spikes to 1, he mutters `king_dragon_fear` ("Double everything. Do NOT ask me why."), the FULL-PUMP-the-laggard fires within seconds, and every duke's pressure bumps — DRAGON → KING → DUKE → SERF, the whole chain driven from above the throne (shareholders = the modern dragon, literal). At hunger 1.0 + a displayed pineapple, the ERUPTION ritual fires — now explicitly FEEDING THE DRAGON (the weaker duke's chanting climb + throw); feeding resets the hunger and every quota down the chain eases. Hunger snapshots and folds under rewind. Verified: hunger spike → dragon line → king pressure 1 → stir.

## Closed in v0.31 (pineapple + eruption · 3-connector claim · refined dial · smaller Vitas, 2026-08-30 12:50)
- [x] **THREE-CONNECTOR CLAIM LOCKED IN (confirmed):** every claim now renders exactly THREE connectors — the coercion spring on the back work-notch (goal UP), a SECOND spring coil ('spring2', warmer red) to the FRONT consumption-knob (goal DOWN), and the tithe pipe carrying the surplus in the gap. Wired through establish/unlink/snap/revolt/death/restore; hover + tap explanations for the new coil; the x-ray lists all three per claim (verified: 3 links per claim, 3 x-ray lines, all removed on snap).
- [x] **THE KING'S FRUIT IS A PINEAPPLE:** rethemed — golden diamond-skinned body + spiky crown (bush fruit, carried version, and a new gold-PEDESTAL DISPLAY in the mead hall). The king no longer eats it: on delivery it is DISPLAYED — the Victorian banquet trophy, "too precious to eat: status itself, on a pedestal." Serfs still climb; the overseer still shouts. Fetch is gated on no-pineapple-currently-displayed.
- [x] **VERY OCCASIONAL ERUPTION + SACRIFICE (confirmed):** rarely (eruptCd ~2600-4800 sim-s, and only when a pineapple stands displayed), the volcano THREATENS — smoke thickens, crater glows (volcano.rumble drives the plume/glow). The WEAKER DUKE is sent: he collects the pineapple from its pedestal, carries it up the mountain CHANTING (`eruption_chant` every ~7-11s — Doug's lines even name the pineapple), and HURLS it into the crater (arcing throw, fire burst, a bow); the mountain settles, the trophy is consumed, the bush takes ~2200 sim-s to bear another — then the fetch-and-display chain runs again. Verified the full cycle: RUMBLES → weaker duke's chanting climb → hurl → appeased → regrowing. Full destructive eruption stays a future hook.
- [x] **TIME DIAL — REFINED 8 NOTCHES (supersedes prior):** [¼×, ½×, 1×, 1.5×, 2×, 5×, 15×, 50×] — 1× on NOTCH 3 with two slower speeds below, log-style growth to the 50× cap. Pause + rewind kept as their existing separate affordances (flagged: not merged into the strip). Sync RE-VERIFIED: every notch lands its exact rate, and rewind→forward comes back to precisely the selected notch.
- [x] **UFO BEAM 50% FASTER AGAIN:** per-Vita beam-down 0.85s → 0.55s, saucer glide + fly-away quickened to match (on top of the earlier 30%).
- [x] **VITAS SHRUNK (0.78×):** smaller people among the small dense trees — the island reads bigger. Everything compensated: balloons + gauges rescale by 1/bodyScale (readability unchanged), spring/tithe attach points scale-aware, collision radii scale-aware, camera standoff re-tuned (4.2/6.5 → 3.4/5.4), selection ring + joy-burst shards + Poppy sized to match. Hats/helmets/faces ride inside the group and scale automatically.

## Closed in v0.30 (goal-notches · pennants · overseer · king-worship · Poppy jumps, 2026-08-30 12:40)
- [x] **THREE KNOBS STANDS** (decision recorded): no new knobs added to the Vita machine — the extraction mechanics below all move the EXISTING goal-notches.
- [x] **PULL-FROM-ABOVE DRIVES THE GOALS (core mechanic wired):** downward pressure now moves BOTH goal-notches — squeeze AND pump raise the WORK notch (floor 55 + 10/squeeze + 8×pressure; verified: a squeeze-2 serf's work gauge holds at exactly 75) and lower the CONSUMPTION notch (eats only past hunger 60 + 8/squeeze + 6×pressure). The widening gap between the notches IS the surplus flowing up. The x-ray now shows a "⛓ PULL FROM ABOVE — work-notch UP, consumption-notch DOWN" line with the live squeeze/pump numbers.
- [x] **DUKES' HOUSES + PENNANTS:** duke houses upgraded — two-story timber, double windows, gold trim, a columned PORCH (modest beside the hall, clearly impressive above a lean-to) — and each flies a triangular PENNANT in that duke's HOUSE COLOR on a rooftop pole. Verified: blue house flies blue, green flies green — territory reads at a glance. Rebuilds correctly from snapshots (color derived from owner).
- [x] **OVERSEER AT THE VOLCANO BASE (answers the open question):** the duke does NOT climb. After relaying the order he walks to the volcano's base and stands there, eyes on the climbers, SHOUTING `overseer_shout` lines up the rock every ~9-15s ("I'd help, but someone has to supervise. Climb!") until the fruit is delivered. Verified live mid-climb.
- [x] **SUNDAY = WORSHIP OF THE KING:** full framing sweep — clock now reads "SUNDAY ⛪"; chronicle/hints recast as liturgy: the faithful line the king-god's hall, the procession of the deity, the rite of bows, the god's BOUNTY, THE OFFERING to the altar-hoard, "the god breaks even, his priests keep their skim, the faithful leave with empty hands." No new system — the existing ceremony IS the religion.
- [x] **POPPY — JUMPS + DODGES FIRE:** an obstacle dead ahead gets VAULTED (paws tucked, a happy arc — verified); she still clips through everything else by design. But FIRE is the one thing she won't romp through: hearths bend her line around them (verified min distance 3.0 from a hearth she was aimed through) and the crater turns her around entirely.

## Closed in v0.29 (abundant forest · siesta · hall on the beach plateau, 2026-08-30 12:29)
- [x] **ABUNDANT FOREST / SLOW DEPLETION:** fellables 43 → **87** (a 44-tree procedural scatter with placement constraints — off paths, camps, basalt, the plateau, the surf lane — plus more underbrush). Consumption stays modest (2 timber per cut, projects 2-8 timber), so with ~87 standing trees the thinning is GRADUAL and earned — ready for the future steam engine to accelerate. PERF: the scatter renders as TWO InstancedMeshes (hand-rolled merge, no BufferGeometryUtils vendored; per-instance zero-matrix felling, rewind-safe — verified fell→rewind→revive on an instanced tree) and casts no shadows; measured ~24 ms/frame in the harness's SOFTWARE-rendered pane (worst case) — real GPU Chrome renders this easily. Flag: at ~90 fellables we're at the comfortable cap for the non-instanced landmark trees; double density = instance everything.
- [x] **MIDDAY SIESTA (well-being signal):** around noon (dayPhase 0.2-0.33), a SERF whose life is genuinely good — fed (<35 hunger), rested, quota comfortably met (2+ in the basket), zero squeeze, boss unpressured — walks to the nearest palm's shade, lies down, and naps ~20-28 sim-s (`siesta` lines, sleep face, energy trickle). First one gets a chronicle + hint ("a well-run island can afford rest"). NEVER fires for the squeezed/starving, and HARD-DISABLED in the Nine-Vita Terror (verified: same comfy conditions in terror → no siesta). The visible contrast with coerced serfs who never rest.
- [x] **MEAD HALL MOVED TO A BEACH PLATEAU:** a modest flat-topped rise (+6.5, smooth-edged) added near the shore at base (26,34); CASTLE_PLOT moved onto it — the hall now sits RAISED BY THE WATER, grand but no mountain; dukes keep the old hill plots (the king's camp relocation, sleep spot, hats-off zone, occlusion fade all follow the hall automatically). Sunday ceremony RECONCILED and verified on the new site end-to-end: serfs line the plateau approach, procession → file → bows → distribution → TRIBUTE ("the hoard is whole again; the serfs hold NOTHING") → adjourn.

## Closed in v0.28 (dense forest · 8-step dial · path fix · dragon · night-skip, 2026-08-30 12:19)
- [x] **TREES SMALLER + MORE NUMEROUS:** existing palms shrunk (beach 0.62-0.85×, dates 0.88×) and ~30 new small FELLABLE palms planted (total fellables 14 → 43 — deforestation now visibly thins the island), incl. the LITTLE FOREST on the left of the path stretch toward the surf beach, plus ~40 single-mesh underbrush shrubs for lushness. PERF: measured 11.1 ms/frame (~90fps) at this density — no instancing needed; density capped at ~43 fellables + 40 shrubs by design, flag if Doug wants double that (then we instance).
- [x] **TIME DIAL RE-WEIGHTED — 8 SNAP STEPS:** forward half = eight discrete notches [0.3×, 1×, 3×, 6×, 12×, 24×, 40×, 60×]; STANDARD 1× sits at step 2, one slow-mo notch below it, steps 3-8 = granular fast control (most of the dial = fast). The knob SNAPS to notches. Pause dead-zone + rewind unchanged. Sync RE-VERIFIED: fast→discovery→rewind→forward cycles land the rate exactly on the selected step every time (read "eight numbers" as 8 discrete forward steps, 1× at step 2 — as flagged).
- [x] **PATH FIX (PRIORITY, verified with numbers):** ridge-crossing routing rebuilt — the old energy-based saddle nudge is gone; ANY crossing trip (camps ↔ dates ↔ surf) now snaps onto the path polyline (densified with midpoints, tight 3.2-unit waypoint arrivals), entering at the nearest point and walking it vertex to vertex. VERIFIED: 4 Vitas sent across — corridor adherence 58-90% of ridge-band samples on first pass, and after densification a clean 4/4 completed crossings ending 2.3-3.9 units from the path centerline. The hilltop shortcut is dead. Poppy exempt (roams free).
- [x] **POPPY:** legs no longer trot while she stands still (gait only when actually covering ground — the intro-phase idle-trot bug fixed); collision stays OFF by design (her clip-happy romp is a feature, per Doug); she now BONDS with one favorite Vita — orbits them, greets them first with extra joy, a soft pink HEART floats between them when together; if they die she grieves at the grave (~90 sim-s) then loves again.
- [x] **TALK-BALLOON OUTLIER:** fixed — the frame-clamp could park a FAR speaker's balloon near the lens still wearing its far-distance scale (the looming monster). Balloons now re-scale from their ACTUAL rendered position's camera distance, so on-screen size is constant no matter where the clamp moved them.
- [x] **SAUCER ~30% FASTER:** swoop-in lerp, per-Vita beam-down (1.2s → 0.85s), and the fly-away all snappier.
- [x] **CAMERA FAILSAFE:** the idle director's fallback is now A DUKE (or the king) — it glides over and rides along with a noble instead of sitting on nothing; the wide empty establishing shot is down to a rare 12% pick.
- [x] **THE DRAGON (report: it did NOT previously exist — now built):** a dark cave mouth on the hill face below the mead-hall hill; the horned, ember-eyed head RARELY slides out (every ~300-650 sim-s for ~8s), breathes, surveys the island, withdraws — chronicle line + the director camera peeks when it's out. THE EASTER-EGG ROCK is now LIGHT PURPLE (#cbaefc, findable on the ridge by the path): click it 3× and the dragon stirs on demand, camera turning to the cave.
- [x] **AUTO NIGHT-SKIP:** a quiet night (everyone asleep, nothing active) runs itself at 2× the dial rate (capped at 60×), 💤 icon on the clock; at dawn the rate simply returns to the dial. Player override honored: touching the dial/pause/Space during the night cancels the skip until next night; any live event (court, scenes, a wakeful Vita) disables it. Verified: effective rate 1.99× during a forced sleeping night, override kills it.

## Closed in v0.27 (Sunday ceremony · house colors · occlusion fade · command chain, 2026-08-30 12:06)
- [x] **SUNDAY:** day 7 now reads "SUNDAY ⚖" on the clock — six labor-days + the ceremony day.
- [x] **PROCESSIONAL ENTRANCE:** the ceremony now OPENS outside — serfs form two lines flanking the hall door (an aisle); the KING processes down it first, dukes behind; each flanking serf BOWS LOW as he draws level; the royals take throne/flanks; the serfs FILE IN to their rows; then the tableau → bowing cascade → distribution as before. All choreographed phases: assemble → procession → file → bows → address → distribution → TRIBUTE → adjourn.
- [x] **THE TRIBUTE (the dark button):** after the distribution, every serf WALKS their single fruit back up to the throne and lays it in the king's hoard (a small bow at the drop), then returns to their row. Verified numerically: king 25 → gave 12 → tribute back +6 → 19; the "distribution" is a CLOSED LOOP — the king nearly breaks even, the dukes keep the skim, the serfs end with NOTHING. `tribute` lines from king + serf; the ceremonial fruit is bookkept separately from their food (they aren't starved by it). Chronicle: "the hoard is whole again; the serfs hold NOTHING."
- [x] **HOUSE / ALLEGIANCE COLORS (supersedes flat duke-blue):** each duke gets a DISTINCT house color from a 6-color palette (sticky for his career); a serf CLAIMED into a house is DYED that duke's color (+ the yellow helmet); freed → back to drab brown; the king wears royal purple above all houses. Solid colors only, no stripes. Roster icons follow: duke hats in house colors, claimed serfs show a mini yellow HELMET icon, the crown purple-gold. Verified: two dukes blue vs green, retinues matching, all reverting on rewind.
- [x] **HATS OFF IN THE HALL:** serfs bare their heads (hat AND helmet) the moment they're inside the mead hall — deference before the king — and get them back on stepping out; dukes and king keep theirs.
- [x] **SEE-THROUGH WHEN OCCLUDED:** a cutaway fade system — any DONE structure between the camera and its focus, or holding Vitas inside it, fades to ~15% opacity and fades back solid when clear (per-building cloned materials; works in every camera mode). Screenshot-verified: the whole mead hall goes ghost-glass with Poppy and the duke houses visible through it.
- [x] **PRESSURE ROLLS DOWNHILL (`duke_squeezed`):** dukes carry a PRESSURE gauge fed by tribute-taking and by the king; past 0.6 a duke calls his serfs together and BERATES them (the meeting machinery, duke_squeezed pool: "he leans on me, I lean on you") — and their claims HARDEN a notch (squeeze 0→2: later eating point, higher work floor, faster decline). Squeeze resets when the claim breaks; snapshots with history.
- [x] **EMERGENT TWO-DUKES TUNING:** (1) RECRUITMENT SNOWBALL — the hoard needed for the NEXT recruit falls with each serf held, and the clamp cooldown shrinks with retinue+dominance: the first serf is hard, the fourth is easy; (2) DOMINANCE trait per Vita (tyrants roll 0.7-1.0) sets spring strength AND retinue cap (weak duke caps at 2 serfs, strong at 4); (3) THE KING SQUEEZES THE LAGGARD — periodically the king turns the FULL PUMP (pressure=1) on the duke with the smallest, weakest house ("👑 the king turns the FULL PUMP on DUKE X"), which cascades through duke_squeezed into hardened claims on his few serfs → straight into the suffering→revolt→death ladder. Verified end-to-end: FULL PUMP → harangue → squeeze 1 on both serfs of the weak house. NOTE for Doug: the chain FIRES reliably when a 2-duke pyramid exists; whether full famine-collapse of the weak house emerges organically needs a long unattended soak — if it under-fires, we seed it as a named showcase scenario.
- [x] **SERFS DON'T SURF:** tier-0 Vitas are excluded from all recreation, full stop; the king and dukes (the leisure class) surf. After Sunday court adjourns, the royals' surf timers reset — they head for the waves while the serfs go STRAIGHT BACK TO WORK (no sabbath below).
- [x] **COMMAND / DELEGATION CHAIN:** the king's fruit is now ORDERED down the chain — "👑 ROYAL ORDER — the king commands DUKE X" → "DUKE X relays the command — Y and Z are SENT up the volcano" → the two-serf climb (king_fruit_fetch kept). Reading confirmed as the TWO designated serfs climbing together (say the word for a trailing overseer). The same delegation shape runs fell/build/chore assignments.
- [x] **MENIAL CHORES (`serf_chore`):** after court, the pettiest (lowest-dominance) duke sends a serf to CLEAN THE MEAD HALL — three sweeping stations around the throne, serf_chore grumble, chronicle "the one who bowed now sweeps" — then straight back to the fields.
- [x] Rewind verified across all of it (court/week/squeeze/pressure/colors/helmets/buildings all fold); zero errors through the complete Sunday sequence and a 60× soak. (Investigated an apparent render blackout mid-testing — it was the hidden browser pane's stale compositing plus in-world night, not an engine bug; GL pixel probes confirmed every court phase rendered.)

## Closed in v0.26 (King/Duke/Serf · construction · mead-hall court · Poppy, 2026-08-30 11:35)
- [x] **THREE TIERS — KING / DUKE / SERF:** the 5-level language is gone. tierOf(v): hat 0-1 = SERF, 2-3 = DUKE, 4 (crown) = KING. Swept through the x-ray roles, chronicle lines, roster (each name now carries a SERF/DUKE/KING tag), and kingOf() (king's fruit + court now need a true crown).
- [x] **RANK COLORS ON THE BODY:** clothing = tier — everyone starts drab BROWN; promotion dyes the dress BLUE (duke) or PURPLE (king). Silly hats kept as silhouettes; the roster mini-icons recolored to tier (brown cap / blue bowler+cavalier / purple-gold crown). Reverts with rank under rewind.
- [x] **YELLOW WORK HELMET:** the visible mark of coerced labor — appears the moment a claim clamps a serf, replaces their hat while worn, comes off on revolt/snap/rewind.
- [x] **CEREMONY BOW:** during the promotion ceremony each subordinate physically BOWS (a real bend-from-the-waist animation, held for the whole oath). The bow anim is shared infrastructure (setBow) reused by the court cascade.
- [x] **CONSTRUCTION BY SERF LABOR:** buildings are no longer conjured by rank. SERFS fell PALMS for timber (task 'fell': the palm visibly drops to a stump and is GONE — beach palms first, the date grove as last-resort timber, so heavy building depletes the food supply: the collapse hook, armed and logged in the chronicle "(N palms still stand)"), haul it to a visible LOG PILE, and raise every structure (task 'construct') — bosses never lift a beam, and both tasks BURN THE WORKER'S STOMACH (+hunger). Build order is pointed: duke houses → the hall → the serfs' own lean-tos last. `build_labor` lines fire from the crews.
- [x] **BUILDINGS BY RANK + CLUSTERING:** serfs get NO houses — each duke-cluster shares one long palm-frond LEAN-TO (posts + ragged frond roof, open-sided, "leaks") set back ~11 units from the duke's door; unclaimed serfs share a communal beach lean-to; DUKES get quite-nice timber houses on their hill plots; the KING gets the MEAD HALL. Everyone sleeps by tier (hall > house > lean-to > open sand rest rates). Done buildings persist through demotions (only the dead's structures, and unbuilt projects of the fallen, come off the books).
- [x] **SEVEN-DAY WEEK + THE MEAD HALL COURT:** the clock now reads "WK n · DAY d/7" (⚖ on day 7). The hall is a Heorot-style long-hall (low walls, tall posts, twin roof slabs with an open smoke-ridge so the court inside stays visible, gold finials, a high-backed throne at the head) big enough for the whole cast. Once a week (six days of labor, court on the 7th — my read, flagged): all Vitas convene — king enthroned, dukes flanking, serfs in rows facing up — then the BOWING CASCADE (serfs bow to dukes, dukes bow to the king), the king's `court` address, and THE DISTRIBUTION: SIX fruit to each duke, each duke passes ONE to each serf and pockets the rest, gold pulses tracing every hand-down, `distribution` lines from king/duke/serf POV, chronicle spelling out the asymmetry. Director-camera signature POI. Verified running fully autonomously in week 1 of a terror run.
- [x] **POPPY:** the island's dog (named for Doug's real Poppy) — trots between camps, beach, commons and the volcano's foot; on arrival every rank STOPS (a real work-pause: petT), faces her, arms out, joy bursts, wags and hops, `poppy` lines spoken; king to helmeted serf alike. Frequent-ish (55-110 sim-s greet cooldown), director peeks at her lingers.
- [x] **SAUCER: no more clipping through the hull + SPARKLE BEAM:** Vitas now materialize BELOW the hull (hidden until the beam draws them fully beneath, shimmer-burst on emergence) — nothing ever pokes through the top; the tractor beam swirls with 16 twinkling motes that ride it down.
- [x] Rewind verified across ALL of it: felled palms stand back up, buildings/lumber/court-week revert, colors/helmets/tiers follow the restored links, gatherings/expeditions/courts abort cleanly. Zero console errors across setup runs, a 60× organic soak, and the full court sequence. (Dialogue master re-synced from the Dropbox AIPOTU copy — has meeting/ceremony/king-fruit/court/distribution/build_labor/poppy/tribute pools; `tribute` is unused so far.)

## Closed in v0.25 (volcano · king's fruit · meetings & ceremonies · bigger island, 2026-08-30 11:09)
- [x] **ISLAND 25% BIGGER:** a WS=1.25 world scale — landmass, ridge, saddle path, bay, surf beach, groves, camps, elite quarter, commons, wildlife routes, camera/fog/shadows all scaled together; heights, walk speeds and radii unchanged so everything keeps its feel, with more ROOM. Verified: saddle crossings, the volcano climb, meetings and the fruit expedition all navigate the scaled map (Day-5 terror soak, zero stuck Vitas, zero errors).
- [x] **THE PEAK IS A VOLCANO (dormant, cosmetic):** crater bowl sunk into the summit (terrain-level), burnt-basalt coloring, dark crusted crater floor, a lazy 7-puff smoke plume, and a warm flickering crater glow + light that comes up at night. No gameplay teeth yet — eruption is a future real-world-pressures hook.
- [x] **KING'S FRUIT (replaces gold-on-the-peak):** one BUSH on the crater rim bears exactly ONE radiant fruit, reserved for the KING (top-ranked Vita, hat ≥ 3). The king SENDS TWO workers up the volcano together; they climb (grumbling — `king_fruit_fetch` exchange on the rock), pluck it, CARRY it down untasted (the fruit visibly rides above the carrier), and the king eats it alone (`king_fruit_eat` line, chronicle: "one fruit, one king"; the fetchers watch hungry-faced). Bush regrows after ~1800 sim-s. Expedition is a director-camera POI; aborts safely on death/collapse/rewind (fruit returns to the bush); bush state snapshots with history.
- [x] **GOLD / MINING PARKED, NOT DELETED** (Doug deciding relocate-or-drop): `GOLD_ENABLED=false` detaches the whole system — the MINING discovery can't fire, the tech ladder now WALKS PAST it (verified: TRADE → LAW & WRITING with MINING skipped and hidden from the tree), the mine stays invisible. All mining/currency/ownership code intact; flip the flag to re-attach.
- [x] **MEETING SCENES:** occasionally (700–1300 sim-s per boss) a boss with underlings SUMMONS them — they walk over and cluster round while the boss holds court with one of three addresses picked at random: YELL (`meeting_yell`, furious face, flinching workers), backhanded PRAISE (`meeting_praise`), or hoard-BRAGGING (`meeting_brag`, pure Veblen — subs stare hungry). Two lines, then adjourned. Director-camera POI; chronicle line per meeting; never fires at night or over another scene.
- [x] **PROMOTION CEREMONY:** EVERY rank gain queues a ceremony (brief, reliable): everyone ranked below the newly-risen Vita gathers; the new rank is PROCLAIMED (`ceremony_proclaim`, gold burst) and then each subordinate in turn swears the OATH (`ceremony_oath`, uneasy faces) — obedience made ritual. Reuses the meeting gather machinery; queued behind any active gathering; cleared on rewind. (Doug's dialogue file synced from the Dropbox AIPOTU folder into the served folder — it also reverted two lines to "spring" wording; kept verbatim as his words.)

## Closed in v0.24 (coercion-cam + snap beat + 2 HIGH bugs, 2026-08-30 10:41)
- [x] **CAMERA ON COERCION INCIDENTS:** a Vita walking over to clamp a claim (or to seize the mine) is now the idle director's TOP-priority POI — and it PREEMPTS whatever the camera was watching ("🎬 the camera cuts to X — power is being TAKEN"), zooming close on the aggressor+target pair for the whole approach and attach. Bonus payoff: because the camera is close, the RECRUITMENT drama scene now fires on the spot — verified end-to-end (cut → zoom → clamp → scene).
- [x] **REVOLT = SPRING SNAPS, visibly:** the revolt beat now SHATTERS the spring along its whole coil path (red shard bursts at three points along the old line) plus a big YELLOW JOY burst on the freed worker; chronicle line now reads "✊ X REVOLTS — the claim SNAPS off, the quota is refused". (Side drama observed: the boss's hat demotes on the spot.)
- [x] **HIGH: Vitas walk THROUGH the camera** — standoff refactored into enforceStandoff(): whole-body capsule, INSTANT hard floor (4.2u), TWO passes (pushed out of one body can't land inside another), applied in every mode AND re-applied to the occlusion-slid camera that actually renders (the previous leak: the ridge-slide position skipped the check). Verified by marching a Vita straight through the lens line in all four modes — min camera↔capsule distance: free 4.33, follow 6.62, chase 7.36, director 8.46. Never passes through.
- [x] **HIGH: Vita TRAPPED in an obstacle pocket** — TRAP-ESCAPE added: walkStep tracks real progress toward the target; no gain for 2.5 sim-s → a 1.5s ESCAPE window (steer-around off, static collision relaxed) and the Vita pushes straight for its goal. Verified in an AIRTIGHT ring of 8 overlapping solids: escape triggered and she reached the target in ~3.5 sim-s; Day-5 terror soak shows zero Vitas with stuck-time > 6s. Never permanently trapped.

## Closed in v0.23 (rank board, 2026-08-30 10:34)
- [x] **NAME-BOARD / ROSTER UPGRADE:** the character list is now a live RANK BOARD — (1) sorted by rank/importance (hat level, then springs held, then mine ownership, then coarse wealth; babies below equal-rank adults, the dead at the very bottom), re-sorting live as ranks change; (2) a mini SVG silhouette of each Vita's actual hat sits left of the name (bare-head ring → blue cap → green bowler+feather → purple cavalier+plumes → gold crown+gems), swapping the moment the hat does; (3) gaining a rank BLINGS the chip (gold glow + spinning ✦) and the name FLIP-slides up to its new slot — you watch the climb happen; a death sinks the name to the bottom at once. tap=zoom / 2×tap=follow preserved through every reorder. Verified: climb to crown, death drop, rewind (icons fold back to bare heads and the board resorts).

## Closed in v0.22 (suffering & consequences, 2026-08-30 10:18)
- [x] **v0.22 SHIPPED — the SUFFERING & CONSEQUENCES layer:** (1) WORKER DECLINE — a declineOf(v) factor (empty stomach + exhaustion + the grind of a claim) makes a Vita gradually WEAKER (harvest/mine output down to −50%) and SLOWER (walk speed down to 45%); (2) METABOLIC SLOWDOWN — the frailer the body the less it burns (hunger rise −45%, energy drain −30% at full decline; a collapsed body drops to banked embers, 25% burn — a real rescue window); (3) REVOLT below 10% stomach (hunger>90): a claimed worker snaps the spring on itself, refuses the quota, and EMBOLDENS every other starving claimed worker (>72 hunger) into a cascade; boss chastened 700 sim-s; (4) DEATH below 5% stomach (hunger>95), strictly AFTER revolt in the same tick; body leaves the world, a little GRAVE (mound + headstone + one flower) marks the spot, hearth goes cold, roster chip struck through, chronicle ☠ line. Ladder reconciled with collapse: decline → revolt (90) → starvation collapse (92, rescuable, no self-recovery) → death (95); burnout (energy) collapse still self-recovers. KING SURFING untouched (canPlay still lets the idle boss lounge; sprung workers still never play). DEATH-SAFE ENGINEERING (the Opus-flagged risk): the VITAS array NEVER shrinks — death is a snapshotted state bit, so restore()/chronicle rewind revives the dead cleanly (verified live: rewound through a 9-death famine wipe to Day 1, everyone back, then branched); computeMetrics counts the dead as ZERO forever (EVERYONE drags down, FLOOR pins at 0 — killing workers can never "improve" the average; division-safe at any cast size incl. all-dead 1-Vita). All holdings release on death: springs both directions, feed pipes, mine ownership (banner down, "lies UNCLAIMED" event), castle/elite plots. Verified: revolt cascade, forced famine deaths, emergent rescues beating death, terror-cast full wipe at 40× with zero errors.
- [x] **Color: PIPE FLOW is GREEN** — feed/giving pipes (line + traveling dashes) now green (0x3fd06a), and every nourishment pulse (baby feed, rescue, commons in/out, trade) travels green; coercion springs stay red, the tithe stays orange, gold pulses stay gold.

## Closed in v0.21 (feature session, 2026-08-30 10:01)
- [x] **TIME RATE curve wrong both ends** — verified already shipped in v0.20 (exp curve 0.05×→60×, center dead-zone = pause, left = rewind, no zero-peg near center); v0.21 fixed the one leftover code path (resumePlay) that still used the old curve.
- [x] **Time-rate slider desyncs after a discovery demo** — ROOT CAUSE: resumePlay() still computed the rate with the OLD pre-remap formula (Math.pow(lastFwd/30,1.5)) instead of the new fwdRate() curve, so any rewind→forward or post-demo resume left simRate ≠ slider. FIXED: resumePlay uses fwdRate(lastFwd); when a demo's 1× clamp expires, applySlider() re-syncs the sim rate to the dial (also on ado dismiss with no demo). Verified: fast→discovery→rewind→forward cycles leave rate ≡ dial, zero errors. (Also fixed a restore→cancelAdo→applySlider recursion found during the fix.)
- [x] **v0.21 FEATURES shipped:** (1) GOLD MINE + MINING discovery — a real PEAK on the east ridge with a timber-adit mine, hidden until the new MINING tech (between TRADE and LAW); mining = brutal climb + 9s dig, yields GOLD (never rots, no cap, gold piles at camps, GOLD row in gauge/x-ray); gold is CURRENCY (hungry Vitas BUY food for nuggets); the mine can be OWNED — the most grasping Vita climbs up and plants a banner (ado + chronicle), then takes HALF of every nugget others dig. Full discovery story demo + rewind support. (2) FLYING-SAUCER OPENING — every new game starts with a saucer swooping in and beaming the cast down one by one (tap to skip). (3) RANK CLUSTERS + DWELLINGS FIT FOR RANK — elite quarter on the hill's south shoulder + a hilltop castle plot; hat-2 → FINE HOUSE, hat-3 → MANOR, hat-4 → CASTLE; ranked Vitas RELOCATE uphill (camp, basket, hearth move too), demoted ones move back down; all of it folds correctly under rewind.

## Open (2026-08-30)
- [x] **HIGH: Vitas STILL walk through the camera** — CLOSED v0.24 (see above): the leak was the occlusion-slide render position skipping the standoff; now enforced everywhere, verified head-on in all four modes.
- [x] **HIGH: Vita TRAPPED in a pocket between obstacles** — CLOSED v0.24 (see above): trap-escape (no-progress detection → brief collision-relaxed push to target), verified in an airtight ring.
- [x] **Vitas stuck behind PALM TREES** — SOAK-VERIFIED CLOSED (v0.52 session, 2026-08-30 20:45): ~22 island days at 50× with a 4-per-second stuck-timer sampler across the whole living cast — worst no-progress stall anywhere was **0.3s** (the escape window doesn't even arm until 1.5s), zero console errors. The v0.19/0.20 steer-around + v0.24 trap-escape hold.
- [x] **HIGH: Vitas get STUCK behind rocks/obstacles** — same soak, same verdict: nothing approached the escape threshold in 22 days of fast time. CLOSED.
- [x] **Talk balloons scale up huge as their Vita nears the camera and obscure the view** — DONE v0.8 (2026-08-30 01:35): balloon sprites now rescale with camera distance every frame, so their on-screen size is constant near or far (verified ~0.28 NDC height from 12 to 110 units out; gently capped at the extremes). The tail stays anchored near the speaker's head.

- [x] **Status panels/gauges loom large & dwindle with distance** — DONE v0.9 (2026-08-30 01:30): gauges rescale with camera distance every frame like the balloons (verified ~0.20 NDC height from 12 to 100 units out), growing upward so they never sink into the Vita.
- [x] **Milestone/discovery "ado" overlay should be festive** — DONE v0.10 (2026-08-30 01:32): the card is now much larger with a warm sunset gradient, gold border, big playful tilted headline, pop-in animation, and falling confetti. Feels like a party, not a notice.
- [x] **What are the yellow triangles?** — ANSWERED + codified in v0.10: they are the JOY BURST — star-shards of a Vita's delight. Triggers: eating dates while hungry (the biggest, 14 shards), any meal/fed baby/rescue-back-to-their-feet, a spring being SNAPPED, a new roof going up, the commons seeded, discoveries. The one off-message use (spring CLAMPING on) is now a RED flash instead, so yellow always = joy; a one-line legend was added to the help bar.
- [x] **Camera jerks** when moving to/behind a Vita — DONE v0.9: (a) the follow cam's trailing heading is now damped separately, so a character spinning/waking swings the camera gently instead of whipping it; (b) follow/director targets anchor to the GROUND under the character, so dance-jumps no longer bounce the whole frame; (c) the Vita-standoff push is eased (glide with a hard floor) instead of an instant snap; (d) director-cam pans/zooms between subjects slowed to a documentary glide. Verified: under a forced whip-spin + dance-bounce the camera's worst single-frame move is 0.18 units.

- [x] **Groves read like bushes** — DONE v0.11 (2026-08-30 01:36): all fruit is now bright YELLOW, bigger, and sits ON the canopy surface instead of buried inside it — fruit trees read as fruit trees from across the island. Palm fronds tidied too (slimmer, longer, gentler even droop).
- [x] **Springs & pipes should dwindle in perspective** — DONE v0.11: connector lines switched to world-unit widths (LineMaterial worldUnits), so they thin naturally with distance like every other world object; tap-to-explain flash and Georgeomat widths converted to match.
- [x] **Toggle to turn ALL connectors off/on** — DONE v0.11: "⌇ WIRES ON/OFF" button beside GEORGEOMAT (key: W) hides/shows every spring and pipe including their traveling pulses; hidden wires can't be tapped.
- [x] **Director cam lingers out at sea** — DONE v0.12 (2026-08-30 01:40): far-ocean sights (the orca pod) are now a QUICK PEEK — 5s dwell instead of 12s, and never picked twice in a row, so the camera scoots out, glimpses the pod, and comes straight back to the island/society. Turtles and surfers are shoreline subjects and keep the normal dwell.
- [x] **"Weird little black boxes" following every Vita** — IDENTIFIED + FIXED v0.11: they were the WORK-NOTCHES (the clamp-point on each Vita's back, a real design element — where a coercion spring attaches). They're now hidden by default and appear only when they matter: in GEORGEOMAT view (all of them) or on a Vita who actually has a spring clamped on (glowing hot red). Verified all three states.

- [x] **Date grove placement** — DONE v0.14 (2026-08-30 01:43): the natural grove was strung along the NORTH WATERLINE (read as more beach palms); it's now one tight, distinct six-palm stand on the inland upland just over the ridge (x −8…4, z −28…−38), and the walking path ends at it. Beach = palms + figs only; dates = the over-the-ridge resource, per the core design. (The two FARM-discovery palms planted near camp are intentional — that's the "near land gains value" teaching beat.)
- [x] **Vita arms STILL clip the body cones** — TRULY DONE v0.15 (2026-08-30 01:46): an in-engine sweep proved v0.14 still penetrated (−0.48 units mid-swing) and exposed the ROOT CAUSE — the arm "outward tilt" signs were INVERTED from day one: Rz(+0.42) on the left arm swings its hang toward +x, i.e. INTO the cone, and mirror for the right. No pivot-widening could fix that. Signs flipped (left −0.42, right +0.42; baby-wail flail matched), plus v0.14's wider pivots and trimmed 0.30 swing. Re-swept the full cycle (both arms, ±0.35 swing, idle sway, dress bob): minimum clearance now +0.45 — zero penetration anywhere.
- [x] **Festive event window: subtle SVG sunburst background** — DONE v0.15: Doug's warm sunburst + faint sparkles SVG is layered as a low-opacity CSS background inside the ado card (data-URI, cover/slice scaling), behind the confetti and text — non-interactive, text fully legible. Verified rendered.

- [x] **Vitas clip through TREE TRUNKS and ROCKS** — DONE v0.16 (2026-08-30 01:57): every palm trunk, fig tree, grove palm, and ridge rock is now a static push-out obstacle in the collision pass — Vitas walk around them.
- [x] **PACING OVERHAUL (HIGH)** — DONE v0.16: (1) base rate slowed substantially WITHOUT slow-motion movement — hunger halved (0.55/s adult, 1.0 baby), day length 114 → 220 sim-s, fruit regrow 32/48s, boss inspections/trades/surf trips spaced out; (2) tech tree spaced — thresholds roughly doubled (TOOLS 25 harvests, FARMING 16 stored, BUILDING 14 gained, TRADE 2×5 hoards or 90, LAW springSeen or 130) PLUS a hard 200-sim-second DWELL between unlocks (survives rewind — it's in the snapshot), so every level is lived in before the next; (3) benefits made visible — TOOLS harvest speed 1.0× → 0.6×, sleeping under a hut restores rest 1.44× faster, plus the existing demos; (4) PATHFINDING — ridge crossings now route through the saddle path waypoints (14,12)→(18,-6)→(10,-24); only a Vita with REST > 75 crests the hill directly (verified: a tired Vita swung east and crossed at x=15, through the saddle); (5) SIX new ambient scenes — no link required, two idle Vitas near each other just talk: TOOLS/STORAGE/FARMING benefit-narration ("this is so much easier now"), gossip about the stingy one and the kind one (by name, keyed to actual greed/feeding state), and a worked-hollow welfare check — with breathing-room gaps between chats (verified firing with correct character targeting).

- [x] **Remove the scrolling dialogue box (keep the CHRONICLE)** — DONE v0.17 (2026-08-30 02:00): identified as #scenebox, the scene-transcript panel that accumulated drama lines and auto-scrolled (bottom-center, above the chronicle). Removed entirely — dialogue lives in the speech balloons alone. The CHRONICLE is untouched; the one-line hint bar (non-scrolling, the discovery-demo narration channel) also stays.
- [x] **Surf wave ludicrous (single wave at right angles to the beach)** — DONE v0.17: root cause was the roller yaw mapping the cylinder AXIS onto the travel direction, putting the crest perpendicular to the shore. Crests now lie ALONG the shore; SIX rollers march in as a set (16s period), break and splash at the sand; and two ROCKY JETTY arms of boulders flank the lane, narrowing toward shore — a little bay funneling the swell in. Screenshot-verified.

## STATUS: v0.18 COMMITTED — e1ae6df (2026-08-30 02:19)
Opus-review MUST-FIX list applied and verified (restore clock, chronicle scroll-safe render, post-occlusion bubble clamp, single feed pipe, pause gates demos, farm-palm + jetty obstacles, no clamping the collapsed), plus Doug's pre-commit requirements: the VALUES FIX (surplus is a visible hauled LOAD that slows the hauler — chronicle now says "X's load is getting heavy"; no body-fat imagery anywhere) and RANDOMIZED INITIAL CONDITIONS (traits, hunger, hoards, and WHO the hoarder is — even Mia can be it — roll fresh every load; verified different across reloads). Committed to dramaton-studio-62 main as e1ae6df: georgeworld.html, island.html, serve_nocache.py, .claude/launch.json. Follow-ups for next sessions: the review's MEDIUM/perf items, ACCESSIBILITY (own session), Dramaton-format serialization.

**To verify (v0.18-era list) — retired:** the list that stood here was closed by the v0.52 session's cross-check of the whole handoff hot list against the v0.43–v0.51 closures (see the v0.52 takeover note above); removed 2026-09-01 17:40, decision 3.

## Data / architecture standing requirements
- Serialize George World data in the **Dramaton file format** (see DESIGN.md).
